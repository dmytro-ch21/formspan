services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vola
      POSTGRES_PASSWORD: vola_dev_only
      POSTGRES_DB: vola
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vola"]
      interval: 2s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
