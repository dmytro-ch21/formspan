name: CI

on:
  push:
    branches: [main]
  # Deliberately unfiltered by base branch. It used to be `branches: [main]`,
  # which silently gave **stacked PRs no CI at all** — a PR based on another
  # feature branch matched nothing and ran zero checks, while still showing a
  # clean "no checks failed" state that reads exactly like passing.
  pull_request:

jobs:
  backend:
    name: Backend (Go)
    runs-on: ubuntu-latest
    # timeout-minutes on every job: GitHub's default is SIX HOURS, so anything
    # that hangs rather than fails burns a runner for an afternoon before
    # anybody sees it. Measured on a recent green run, the slowest job here is
    # ~1.8 min, so 15 is roughly eight times the worst and twenty-four times
    # under the default. This bounds the JOB; F12's `-timeout 3m` bounds a Go
    # package and jest's `testTimeout` bounds a mobile test — three different
    # granularities, and only the outermost one catches a hang in setup,
    # install or a runner that wedges. Measured: a 25s block at MODULE scope in
    # a jest file runs to completion and the test reports PASS — `testTimeout`
    # never sees it, because no test is running yet. That class is invisible
    # until something bounds the job.
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: vola
          POSTGRES_PASSWORD: vola_test
          POSTGRES_DB: vola_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U vola"
          --health-interval 2s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgres://vola:vola_test@localhost:5432/vola_test?sslmode=disable
      TEST_DATABASE_URL: postgres://vola:vola_test@localhost:5432/vola_test?sslmode=disable
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: backend/go.mod
          cache-dependency-path: backend/go.sum

      - name: Check formatting (gofmt)
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "The following files are not gofmt'ed:"
            echo "$unformatted"
            exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build ./...

      - name: Run migrations
        run: go run ./cmd/migrate up

      - name: Test
        # -p 1: packages share ONE database, and several tests assert global
        # counts (SELECT count(*) FROM techniques). Run in parallel, another
        # package's fixtures are inside those counts. Latent on main and
        # reproducible the moment a second package seeds library rows —
        # measured 3 failures in 6 concurrent runs, 0 with -p 1.
        #
        # -timeout 3m: Go's default is 10 MINUTES per package, and a test that
        # hangs rather than fails burns all of it. Measured, not hypothetical —
        # F11 deleted a deferred `tx.Rollback` and the leaked connection kept
        # `pgxpool.Close` waiting forever, so the package could not exit even
        # after its assertions had already named the cause. Slowest package here
        # is ~2s locally and ~45s under the parallel-session contention
        # CLAUDE.md documents, so 3m is roughly four times the worst seen and
        # still bounds a hang at a third of the default.
        run: go test -p 1 -timeout 3m ./...

      - name: Build Docker image
        run: docker build -f Dockerfile -t vola-api:ci .

  web:
    name: Web (Next.js)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint OpenAPI spec
        run: pnpm run lint:openapi

      - name: Lint
        run: pnpm run lint:web

      - name: Typecheck
        run: pnpm run typecheck:web

      - name: Test
        run: pnpm run test:web

      - name: Build
        run: pnpm run build:web

  admin:
    name: Admin (Next.js)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint:admin

      - name: Typecheck
        run: pnpm run typecheck:admin

      - name: Test
        run: pnpm run test:admin

      # apps/web and apps/admin hold deliberate copies of the brand components.
      # A comment saying "change both" held for exactly one commit, so the
      # invariant is enforced here instead. Lives in the admin job because that
      # is where the second copy was introduced; it compares both files.
      - name: Brand copies have not drifted
        run: pnpm run check:brand-copies

      - name: Build
        run: pnpm run build:admin

  scripts:
    name: Scripts (Python)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      # `fetch-depth: 0`, and it has to be. `check-tasks-integrity.py` compares
      # against the MERGE BASE with `origin/main`, and a merge base needs shared
      # history: a shallow checkout plus a `--depth=1` fetch of main gives two
      # commits with no common ancestor, so `git merge-base` finds nothing.
      #
      # That was tried and CI caught it — the first version fetched main at
      # depth 1 and the step failed with "cannot read docs/TASKS.md at the merge
      # base". Which is the designed behaviour working: the script FAILS rather
      # than skips when the baseline is unreachable, so a misconfigured fetch
      # turns the build red instead of quietly disarming the check. Weakening
      # either half — this depth, or that refusal — puts it back.
      #
      # Full history for one file is the price. This is the cheapest job in the
      # matrix (no toolchain, no install), so it is the right one to pay it in.
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      # Belt and braces. `fetch-depth: 0` gives the history a merge base needs,
      # but `actions/checkout` does not promise a `refs/remotes/origin/main` ref
      # on a PR checkout — that lands detached on `refs/pull/N/merge`. This
      # creates it, is a no-op when it already exists, and carries NO `--depth`,
      # which is precisely what broke the merge base the first time.
      - name: Make origin/main available (for the TASKS.md merge base)
        run: git fetch --no-tags origin main:refs/remotes/origin/main || true

      # No actions/setup-python on purpose. The check is stdlib-only and
      # version-agnostic, the runner image ships python3 already, and every
      # extra action is one more thing to download before any of this repo's
      # code runs — the failure mode that took out three jobs on 2026-08-06
      # ("Failed to resolve action download info").
      - name: Python version
        run: python3 --version

      # docs/TASKS.md is the one load-bearing file every other check is blind
      # to — it is prose to lint, typecheck and every test suite. A task
      # silently reverting from [x] to [ ], a duplicated id, or a dropped line
      # all pass everything else and merge. All three nearly landed in one
      # evening on 2026-08-19, on branches that were green: two from rebase
      # resolutions taking a side wholesale, and one from a CLEAN AUTO-MERGE
      # that produced no conflict to notice.
      - name: Task list integrity
        run: python3 scripts/check-tasks-integrity.py

      # The grip vocabulary is duplicated in Go, mobile and web because there
      # is no shared package to put it in. This is what makes that survivable:
      # drift fails here rather than reaching an athlete as a 400 they cannot
      # explain, or as a grip the picker silently refuses to offer.
      - name: Grip vocabulary parity
        run: python3 scripts/check-grip-parity.py

      # The evidence-based rate bands are duplicated between anthropometry.ts,
      # which judges an observed rate, and nutrition/target.go, which derives a
      # calorie target from the same band. Neither can be deleted: the mobile
      # copy has to work with no signal, the Go copy has to aggregate training
      # the phone does not hold. Drift is worse than the grip case because both
      # halves stay plausible — the app would report "on target" against a
      # number the target itself never used.
      - name: Rate band parity
        run: python3 scripts/check-rate-parity.py

      # `verify` is one long `&&` chain in package.json that every task appends
      # to, so two branches touching it always conflict — and always in a way
      # where taking one side looks like a correct resolution while silently
      # deleting the other's link. A dropped link is not a broken build: it is
      # a gate that stops running while `verify` still exits 0. This asserts
      # every gate is either in the chain or explicitly excluded with a reason
      # and run here instead.
      - name: Every gate is reachable
        run: python3 scripts/check-verify-chain.py

      # The detector for "this PR received ZERO check runs", which is
      # indistinguishable from every check passing. This step runs its OFFLINE
      # self-test only — the live half needs an authenticated `gh` and a PR, and
      # is `pnpm run ci:checks`, run by hand or by /pre-merge.
      #
      # It is here because the self-test is what notices `ci.yml` itself
      # changing shape: a sixth job, a `matrix:`, a reformat that breaks the
      # parser, a renamed check. Every one of those is an edit to THIS file, so
      # leaving the check only on an author's laptop puts the guard furthest
      # from the thing it guards. It was the one python gate absent from CI;
      # found in review.
      - name: The zero-checks detector still detects
        run: python3 scripts/check-ci-checks.py --self-test

      # The telemetry buffer is duplicated between apps/mobile and apps/web
      # because a shared workspace package was built and measured worse here —
      # four bundlers to satisfy, two of them already failing, and no precedent.
      # Drift on `redact()` in particular is a privacy control rotting
      # silently: an allowlist that gains a key on one platform and not the
      # other is one platform shipping a field the other considers private,
      # with nothing looking wrong on either side.
      #
      # `python3` directly, like its three siblings — this job deliberately
      # installs neither Node nor pnpm (see the comment on the job), so a
      # `pnpm run` here fails with `command not found`. Caught by the
      # pre-merge checker before it ever reached a runner.
      - name: Telemetry buffer parity
        run: python3 scripts/check-telemetry-parity.py

      # Two deadlines bound one request, in two languages, and what matters is
      # the ORDER between them rather than either value: the server must answer
      # before the phone gives up, or the athlete receives no status at all and
      # the app can only say it could not reach the server. That is N92 (#433).
      #
      # Unlike its three siblings above, this one demands the copies DIFFER, and
      # by a margin — the two clocks do not start together, so a nominal gap is
      # spent on the upload before the handler's clock has begun.
      #
      # It nearly shipped broken: N92's constant was written at 45s before N55
      # (#448) gave the client a deadline, and N55 chose 45s too. The rebase
      # turned a deliberate margin into an exact tie, both suites stayed green,
      # and nothing else in the build had an opinion.
      - name: Request timeout parity
        run: python3 scripts/check-timeout-parity.py

      # `apps/web`'s units module is GENERATED from `apps/mobile`'s, and this is
      # what makes that a guarantee rather than a comment. Units are read on
      # nearly every screen in both apps, and the two hand-maintained copies
      # this replaced had already drifted — web was missing all four fluid
      # functions, so mobile could render a volume in the athlete's own units
      # and web could not. Nobody decided that; it is what two copies do.
      #
      # Unlike its four siblings above, this one can also FIX what it reports:
      # `pnpm run units:write`. It is still a check here, because a generator
      # nobody runs is a source of truth nobody has.
      #
      # `python3` directly, like the rest of this job — no Node or pnpm is
      # installed here, so a `pnpm run` would fail with `command not found`.
      - name: Web units module is generated and current
        run: python3 scripts/sync-units.py

      # The other half of N105, and the one the four tickets queued behind it
      # will run into: a single source of truth for units is worth nothing if
      # screens print `kg` themselves. `app/(tabs)/goals.tsx` called `useUnits()`
      # on line 186 and then wrote a literal `kg` in eight places below it —
      # including the goal weight and the shortfall against a competition
      # deadline. The module was being bypassed a few hundred lines under a call
      # to itself.
      #
      # The allowlist is keyed on a LINE SNIPPET, not on a file. Keyed on a file
      # it excused every `kg` in `goals.tsx` because one legitimate `g per kg`
      # lives there — found by mutation-testing the check, not by reading it.
      - name: No hardcoded unit literals
        run: python3 scripts/check-unit-literals.py

      # The dictation eval corpus (N33/N34) is hand-authored, and a hand-authored
      # expectation is exactly as capable of being wrong as the model it grades.
      # An expected tag naming a technique id that does not exist — or writing a
      # category the technique does not derive — scores a CORRECT model as wrong,
      # permanently, and nothing else in the repo would notice.
      - name: Dictation eval corpus
        run: python3 scripts/check-dictation-evals.py

      # The evidence latch (N456) decides whether a closing issue still owes a
      # human-run check, and it runs on `issues:` events where NOBODY IS
      # WATCHING — no PR, no reviewer, no red tick anyone would notice. Its own
      # workflow self-tests on every event for that reason, and this step is the
      # other half: it puts the guard on the PR that CHANGES it, which is the
      # only moment a person is looking.
      #
      # What it protects is a positional rule measured against the live corpus:
      # a checkbox OPENING with the marker is a criterion, a line merely
      # mentioning it is not. Three real lines in this repo are mentions, two of
      # them inside checkboxes, and a naive substring match reopens all three
      # forever — including every future ticket that discusses this feature.
      - name: Evidence latch decides correctly
        run: python3 scripts/evidence-latch.py --self-test

      # The `.vola-agent/` policy contract is what the AI-SDLC dev engine reads
      # before touching a ticket: human-gated paths, raise-only risk rules, the
      # context map, the ticket schema. A typo'd path in human_gate.paths gates
      # nothing and no other check can see it. The script self-tests on every
      # run (mutates copies of the real files and asserts each is caught), so
      # this step also proves the validation can fail.
      - name: Agent policy contract
        run: python3 scripts/check-agent-policy.py

      # Until this job existed, NOTHING in this repo read a .py file: no CI
      # step, no `verify` link, no pyproject/ruff config anywhere. The content
      # pipeline is Python, and the script that imports the whole catalog is
      # run rarely and at exactly the moment being wrong is expensive.
      - name: Syntax
        run: python3 scripts/check-python-syntax.py

  mobile:
    name: Mobile (Expo)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm run lint:mobile

      - name: Typecheck
        run: pnpm run typecheck:mobile

      # jest's worker budget is sized against this machine, so print what the
      # machine actually is. Issue #409 was exactly the failure of not doing
      # that: `apps/mobile/jest.config.js` carried no `maxWorkers` and CLAUDE.md
      # justified it with "CI runs one instance, where 9 workers is right" — a
      # number measured on a 10-core Mac, for a job that has never run on one.
      #
      # `os.cpus().length` is the figure jest divides, and it counts THREADS.
      # `lscpu` is here because it is the one that disagrees: the runner reports
      # 4 CPUs and 2 physical cores, and the gap between those two numbers is
      # the entire bug. Three lines of output on every run is the cheapest
      # possible defence against re-deriving a worker count from a spec sheet.
      - name: Runner capacity (what jest sizes its pool against)
        working-directory: apps/mobile
        run: |
          echo "nproc: $(nproc)"
          # The `|| lscpu` is load-bearing twice over, and neither reason is
          # obvious. (1) A bare `grep` that matches nothing exits 1, and the job
          # shell is `bash -e` — a diagnostic step that can turn the build red
          # would be worse than the flake it documents. (2) GitHub's ARM runners
          # print "Core(s) per cluster" rather than "per socket", where the
          # alternation would still match on `CPU(s):`, exit 0, and SILENTLY
          # drop the physical-core count — the one number this step exists to
          # surface, at the exact moment (a runner change) it exists for.
          lscpu | grep -E '^(CPU\(s\)|Thread\(s\) per core|Core\(s\) per (socket|cluster)):' || lscpu
          free -m | head -2
          # Prints the RESOLVED worker count, not just what jest would default
          # to, so the step verifies the cap rather than merely describing the
          # machine. If these two ever read the same number on CI, the guard in
          # jest.config.js has stopped firing.
          node -e "
          const os = require('os');
          const cfg = require('./jest.config.js');
          const logical = os.cpus().length;
          console.log('os.cpus().length      =', logical);
          console.log('jest default would be =', Math.max(logical - 1, 1));
          console.log('jest.config resolves  =', cfg.maxWorkers ?? '(unset -> jest default)');
          "

      - name: Test
        run: pnpm run test:mobile
