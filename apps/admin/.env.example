# Copy to .env.local for local dev (already gitignored).
# Railway sets this directly per-environment for staging/production.

# Where the admin app fetches the API from (unused so far — the shell has
# no backend wiring yet, this is here for when that lands).
# dev: http://localhost:8080
NEXT_PUBLIC_API_URL=http://localhost:8080

# Same Clerk application as apps/web — the admin console reuses the
# customer Clerk instance rather than standing up a separate one.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_key_here
CLERK_SECRET_KEY=sk_test_your_key_here

# Comma-separated allowlist of emails permitted into /users. Checked
# server-side in app/users/layout.tsx — no roles/orgs yet, just a list.
ADMIN_EMAILS=you@example.com
