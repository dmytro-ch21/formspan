# TASKS

The open list. One line per task, a stable id, a checkbox.

## How to use it

- **Claim before you work.** This list is priority-ordered, so every session picks the same top line —
  two full rounds were lost that way in one afternoon. Run `gh pr list --state open` first (a draft PR
  is a claim), then claim with an empty commit and a draft PR titled `[claim] <ID> — <task>` before
  writing code. A check cannot see unpushed work; `gh pr list` is the one channel every session sees.
  Full rationale in [CLAUDE.md](../CLAUDE.md#claiming-hard-rule).
- **Mark done in place**: `- [ ]` → `- [x]`, and append ` — done <PR or date>`. Don't delete the line; a
  finished task is the record that it was considered.
- **Add a task** at the end of its section with the next free id. Ids are never reused, so
  a commit message or a comment can say "closes W2" and still mean something in a year.
- **One line per task.** Two agents editing this file on different branches is normal and the
  conflict is then a single line, resolvable without reading the whole file. Put the detail in
  [decisions/history.md](decisions/history.md), not here.
- **Ordered by what an athlete would notice**, not by effort. If you disagree, move the line and
  say why in the PR — the ordering is a claim, not a formality.

Sections: **W** wrong now · **T** traps · **F** worth fixing · **N** new · **L** low · **H** housekeeping.

---

## W — Wrong on screen right now

These contradict each other or overstate what the athlete did. Visible without looking.

- [x] **W1** — 1RM estimate counts spotted reps. Nothing reads `soloReps`, so a spotted 8×102.5 estimates ~127 kg where honest solo-5 is ~115; also sets rep PRs not completed unaided. Done together with T1 — #231. (#226)
- [x] **W2** — A drop counts as a set in the Sets tile but not in the row numbers, so one screen shows both answers. Cross-stack: `localVolume` must match the server, and the same figure feeds Today and the calendar. — done #238, a drop no longer counts as a set but still contributes volume (#227)
- [x] **W3** — Per-side load is right everywhere but unexplained: no "enter one dumbbell" hint, no `30 kg × 2 = 60` on a logged set. The athlete can do that maths and conclude the app is wrong. — done #241, the weight field says "per hand" and the row shows "(60kg total)". (#224)
- [x] **W4** — The web sessions list shows two "Working sets" figures on one page under two different rules. The totals strip reads the server's `working_sets` (drops excluded, #238); each `SessionRow` recomputes `s.completed && s.set_type !== 'warmup'` client-side (drops included). Same label, same screen, two answers — W2 exactly, still alive on web because #238 consolidated the MOBILE clients only. — done #241, together with a THIRD bug on the same reduce (it also dropped `load_factor`, halving every dumbbell session). All of it now lives in `sessionVolume()` in `lib/api.ts` with nine tests, because a figure compared across screens must not live in a screen. (#238)
- [x] **W5** — The lunge / split-squat / step-up family disagrees with itself about implement count, and at least one side of every pair is wrong. Every **kettlebell** lunge doubles; every **dumbbell** lunge except `dumbbell-lunge` does not — same movement, opposite factor by equipment. Worse within one implement: `kettlebell-split-squat` ×2 vs `kettlebell-bulgarian-split-squat` ×1. Provable from internal inconsistency alone, but which side is right is a product call (is a dumbbell lunge one dumbbell or two?), so it wants a decision before ~14 rows move athletes' tonnage. Found by review of #254. — done #260. **The rule was wrong, not the rows**: the factor was derived from `load_mode AND NOT is_unilateral`, reading "one LIMB" as "one IMPLEMENT", so a dumbbell walking lunge (two implements, one leg) could not be expressed and whoever classified it had to pick which error to ship. New `exercises.implements` carries the factor; `is_unilateral` goes back to meaning only the reps hint. 8 rows' tonnage changes. (#254)

## T — Traps set for the next change

Each compiles, passes its tests, and is wrong. Read before starting the related work.

- [x] **T1** — Add `assisted_reps` to `RecentEfforts`, `BestOneRMs`, `bestOneRMSets` and `Records` SELECTs **before** wiring progression to `soloReps`. They don't select it, unrecorded reads as all-solo, so a progression-only change silently reads full reps. — done #231 (#226)
- [x] **T2** — `createWithin` never writes `load_mode`, so every console-authored dumbbell exercise starts `total` — the halving bug, for new content. No endpoint can correct a row today. — done #244: INSERT and UPDATE both write it, the console has a Load mode select, and an unknown value is a 400 rather than a coercion to `total`. (#224)
- [x] **T3** — Any new `session_sets` column needs mobile pass-through **before** an authoring surface. The server replaces sets wholesale, so a phone shape that doesn't know a column wipes it on first edit. Applies to grip (**N1**). — done #248 with N1. Two corrections worth keeping: the danger is **not** only mobile (any client that PUTs sets), and the worst case is the SERVER's own read — miss `grip` in `attachSets` and every correct client PUTs back a set the server failed to hand it. (#226)
- [x] **T4** — Adding a fifth `grip` value would be ERASED by stale clients. **Fixed by making the server's refusal machine-readable instead of guessing locally.** `repairSet` nulled any grip outside mobile's own union — right for garbage, wrong for a value a newer server added: an old build read a valid `mixed`, nulled it, and the wholesale PUT wrote that null back over real data, silently. The client now keeps what it holds; the server rejects an unknown grip with its own code `invalid_grip` (not `invalid_input`), and the push drops the grip and retries on that code alone. **T4's own suggested fix — "repair after a 400 that names grip" — was not takeable**: the message is the only thing identifying grip, and matching messages is what api-conventions.md forbids. Hence the new code. **N9** (`mixed`/`hook`) is unblocked. (#256)

- [x] **T5** — **`plan.ts`'s closing compare-and-swap was unheld, and the tombstone half was worse than unheld: it was held by a clock race.** `pushRow`'s `UPDATE planned_sessions SET dirty = 0 ... AND updated_at = ? AND deleted_at IS NULL` is what stops an edit — or a delete — that lands mid-push being marked as already sent. Delete either clause — the `updated_at` one **with its binding**, since the clause alone is a parameter-arity error that measures the harness rather than the guard; the tombstone clause has no binding — and the suite stayed green, **1280/1280**. **Correction to this line as originally filed:** the `deleted_at` half was not held by *nothing* — `deleting DURING an in-flight create` catches it, but only when `planSession` and `unplanSession` land in the SAME millisecond, since otherwise `updated_at` declines first and the tombstone clause is never reached. Measured with the clause removed: **5 of 6 runs of that file alone and 0 of 1 under the full suite here, then 11 of 12 and 4 of 4 on review an hour later** — so read no regime into it: the verdict tracks machine load everywhere, varies by machine and hour, and nothing declares it. CI's own regime was never measured. It read as covered and its coverage was a coin toss, which is the worse failure, because nobody re-measures a guard that looks green. Both are pinned deterministically now: the edit case forces a newer `updated_at`, the delete case writes the tombstone with the real `unplanSession` and then closes the millisecond by hand — 12 of 12 with the clause removed, and it fails under the full suite too. A third case pins that a clean push still goes clean, or "never clear dirty" would satisfy both. (found reviewing #256, #262) (#265)
- [x] **T6** — **A delete landing mid-push was silently discarded in BOTH session and workout sync — the guard `plan.ts` had and these two did not.** `sessionStore.ts`'s closing compare-and-swaps carried only `AND updated_at = ?`, while `deleteLocalSession` and `deleteLocalWorkout` stamp `deleted_at` and `updated_at` from the same wall clock and both push functions branch on the SNAPSHOT's `deleted_at`. So a delete landing in the same millisecond matched the swap, the row was marked clean, and the push loop's `dirty = 1` SELECT never saw it again — gone from the phone, alive on the server, `pending` reading zero, nothing retrying. **The docstring on `deleteLocalSession` asserted the opposite** ("the interleaving is safe because the tombstone bumps `updated_at`"), which is why nobody looked. Fixed by adding `AND deleted_at IS NULL` to both, alongside the existing clause rather than instead of it, and pinned by `tombstoneRace.test.ts` — reverting either fix, or dropping the `updated_at` clause, each turns exactly the matching test red. (#267)
- [x] **T7** — **A rename landing mid-push is lost PERMANENTLY, and unlike T6 it never self-heals.** `pushRow` clears `name_dirty = 0` in its own statement with **no compare-and-swap at all** — no `updated_at`, no `deleted_at` — before the terminal swap runs. So a rename landing during a push of an earlier rename has its outbox flag cleared; the terminal swap then correctly declines on `updated_at`, the row stays dirty, and the next pass sends sets but never the name, because `name_dirty` is now 0. Local keeps the new name, the server keeps the old, and the pull's newer-than guard stops reconciliation — **permanent silent divergence**, not the one-cycle delay T6 cost. The workout path does NOT have this: it clears `name_dirty` only inside its guarded terminal swap, which is the shape to copy. Found by review while checking T6. (found reviewing #267) — done #270. Fixed by the shape T7 names: `name_dirty = 0` folded into the guarded terminal swap, so a declined swap leaves the row owing BOTH the sets and the name. Reproduced first — the test asserts the server ends up with the new name after the race, not just that a flag survived, because the flag is a proxy and the name is the thing lost.
- [x] **T8** — The sessions upsert now carries both guards. The clause is `(excluded.dirty = 1 OR local_sessions.dirty = 0)` rather than a bare `dirty = 0`, as a stated invariant rather than a hot-path need — review corrected me here: athlete edits are direct UPDATEs that never reach this upsert, so an unconditional guard would behave identically today; the disjunct is a backstop for a future caller. Pinned by a real-SQLite fixture that interrupts the pull's own SELECT to land the edit in the gap. **The tombstone test passed for the wrong reason** until the mutation run — a dirty tombstone is declined by the new clause, so only a CLEAN one exercises the old one. (#271)
- [x] **T9** — **`apps/web`'s sequences list says "· reference" off `!s.editable`, which is the F7 inference and is sound only by accident.** `editable` is owner-is-caller, so it means "not yours" and not "VOLA's" — exactly what put a stranger's curriculum on two mobile strips. It is correct TODAY only because `sequence`'s `visibleTo` (`postgres.go`) has no public arm: you can see your own and VOLA's, nothing else, and sharing is by copy. **The day sequences gain a public arm — a browse surface, a `visibility` column, anything — that label becomes a lie in the same breath**, and nothing will fail. The fix is the one F7 landed for curricula: a separate `official` field carrying `owner_user_id IS NULL`, filtered on positively. Noted at what were thought to be both call sites (`lib/api.ts`'s `Sequence.editable`, and the list page). Found by review of #285. **A THIRD site was missed** — `sequences/[id]/edit/page.tsx` renders "This is a reference sequence and cannot be edited" off the same `!s.editable`, with a comment asserting "A VOLA-authored reference chain". Found by review of #288; enumerated here so the eventual fix does not stop at two. — done #289. `official` on `Sequence`, `owner_user_id IS NULL`, same shape and same bidirectional CHECK (`bjj_sequences_source_matches_owner`) as curricula's. All three sites converted, and the edit page's split is the interesting one: the GATE stays on `editable` (you may not edit it whoever wrote it) and only the MESSAGE moved, since "this is a reference sequence" was the authorship claim. **The first version of the test was worthless and the mutation said so** — with no public arm, `!editable` and `official` coincide on both a VOLA row and your own, so `Official = !Editable` passed. It takes a THIRD row, a stranger's, which no endpoint can return today; the test scans the real column list without `visibleTo`, which is the shape a public arm would create.


## F — Worth fixing

- [x] **F1** — The social feed is a `KeyboardAwareFlatList` now, not a `ScrollView` holding every card ever loaded. Header, empty state and the "Show older" footer moved to the list's own slots; the three-way loading/error/empty distinction had to be re-made inside `ListEmptyComponent`, because `data` is `[]` for both "loading" and "quiet". (#210) — done
- [x] **F2** — Tapping Share now opens the card at readable size with Share / Not now, instead of going straight to the share sheet whose only preview is a ~40pt thumbnail. Lives in `ShareCardHost`, so the celebration, the finished strength session and the BJJ class all inherit it. The off-screen capture card stays the capture source — that path is measured, and a preview inside a `Modal` is exactly the "is it laid out" question that produces blank PNGs. (#210) — done
- [x] **F3** — **Eleven fixed, the sweep is not finished.** Seven single-implement movements (`svend`, `hip-thrust`, `glute-bridge`, `russian-twist`, `offset`) were `per_side` while their identical peers (`goblet-squat`, `halo`, `pullover`) were correctly `total`; the two `alternating-dumbbell-*` doubled a dumbbell they move one at a time; `double-dumbbell-kickstand-deadlift` counted ×1 because `is_unilateral` was true about the STANCE. A name guard now fails on all eleven if reintroduced. Was left open pending W5, which has since landed. — done #260: the sweep found no remaining internal contradictions in the catalog, but found the GUARD had stopped working. (#224 #241 #254)
- [x] **F4** — Share card exported at 3× its intended size. `captureRef`'s `width` is POINTS and the renderer multiplies by device scale, so 1080 became 3240px/10.5 MB on a 3× phone — the density dependence its own comment claimed to prevent. Measured 1080px/1.6 MB after. (#232) — done
- [x] **F5** — `blockedRows` filters tombstones now, in both queries, and both deletes clear `last_error`. Less cosmetic than it read: the repair screen's primary action is "Open the session", and `readLocalSession` filters tombstones, so a deleted blocked row offered a button that navigated to a screen showing nothing. A failing DELETE is not hidden — `countPendingSessions` deliberately does not filter tombstones, so the badge still reports it. **Filed as a duplicate `F4` by #267 and then lost entirely when #270 resolved a conflict over it** — re-filed here under a free id, because an id that names two things and a line that vanishes both defeat the point of the list. (#273)

- [x] **F6** — The set editor was twelve flat chips and two shrink-wrapped dashed buttons. Set type and grip are now two `OptionSelect` controls **side by side on one line**, each stating its answer and opening a popover **over itself** so the editor never reflows; the per-set timer target has no switch at all — the row's own clock arms one when dim and starts it when accent; `+ Set` / `+ Drop` share the row evenly on a solid hairline, because iOS renders `borderStyle: 'dashed'` with a `borderRadius` as a squared, uneven box and no value tunes that. Every option answers a long press with what it means (`lib/setGuide.ts`) inside the same popover, reachable under VoiceOver as a custom action since a long press is not forwarded. Deliberately does NOT touch `setMode`'s per-exercise reps↔time switch — that is a different concept and moving it per-set was considered and declined. — done #275
- [x] **F7** (#285) — **`editable: false` does not mean "VOLA authored it"**, and two strips now read it that way. `Editable` is computed as owner-is-caller, so every OTHER athlete's public curriculum is also un-editable — and `track` is unvalidated on `POST /v1/curricula` while `owner_user_id` is deliberately `json:"-"`, so a stranger publishing a curriculum with `track: "syllabus"` and `belt: "white"` appears on mobile's "What each belt should know" wearing a belt word, and the same trick works on the Plan tab's Roadmaps strip. Pre-existing on the Plan strip (#218-era) and extended to one more surface by #282; found by review of the latter. **The client cannot fix it** — it has no discriminator. Server-side options: serialise `owner_user_id IS NULL` as a `vola` boolean, or reserve `track` values for seed-owned rows and validate on write. One fix closes both strips. (#282) **Fixed by the first option**, as `official` rather than `vola`: the palette is imported as `vola` in nearly every mobile component, and the schema already calls this concept official (`curricula_official_is_public` and its twin on workouts). `track` is deliberately left unvalidated — the migration and the domain type both document it as a grouping hint and never a gate, and reserving values would not reach rows already published. Both strips now filter on the positive fact, and the Roadmaps filter moved into `lib/syllabuses.ts` beside the syllabus one, because keeping them apart is how they came to disagree. (#285)
- [x] **F8** — **The web curricula list still read `editable` as authorship.** F7 closed both mobile strips and left `/dashboard/curricula` open: its Shared tab is `(all).filter((c) => !c.editable)` grouped by `track`, so a stranger's public curriculum with `track: "syllabus"` lands under the "Reference syllabuses" heading, and with `track: "belt"` under "Belt roadmaps". The `official` field F7 adds is on the wire already; the web `Curriculum` type does not declare it. Same hole — but **not the same fix**: the strips drop non-official rows because they advertise VOLA content, and this page exists to browse other people's lists, so hiding them would remove the feature to fix the label. Instead a `track` is only BELIEVED from an official row; a stranger's keeps its place under "From other athletes". `trackSections` moved to `lib/curriculumSections.ts` to be testable at all — the same "it could not be guarded where it lived" that #285 hit on mobile. (#285 #288)
- [x] **F9** — **A reference sequence tells you to copy it and nothing can.** The edit route's refusal message said "Copy it to make it yours" — there is no copy affordance for sequences anywhere in `apps/web`, and no endpoint behind one: `sequence.CopyTo` is reachable only by ACCEPTING a share from a friend, never for a chain you are merely reading. Workouts have the real button beside the same sentence, which is presumably where the copy came from. The promise is removed (#289); the affordance is the actual gap — a reference chain you cannot adapt is a chain you can only admire. Needs an endpoint (`POST /v1/sequences/{id}/copy`, or widening the share-accept path) plus a button on the detail page. Found by review of #289. — done #294. `POST /v1/sequences/{id}/copy`, and the repository method **delegates to the existing `CopyTo`** rather than reimplementing it: that function already re-applies `visibleTo` and renumbers steps in one statement, and passing the caller as both sharer and new owner IS the self-copy case. Visibility gates it, not ownership — you may copy anything you may read. The button sits where Edit does not, so a chain you cannot edit is no longer one you can only look at, and the edit route's instruction is back now that something is behind it. Not idempotent on purpose: twice gives you two copies. **Review found three vacuous assertions and two client lifecycle bugs** — the worst being that a surviving mutation was read as evidence about the mutation rather than about the tests (it broke copying a chain you OWN, which nothing covered), and that `router.push` into the same `[id]` segment REUSES the component, so the button stuck at "Copying…" after pressing Back.
- [x] **F10** — **The workouts detail page copies client-side and gets stuck doing it.** Two defects, both recorded when #294 fixed the same pair on sequences and neither touched then. (1) The copy is `createWorkout` then `replaceItems` — two calls, so a failure between them strands an EMPTY workout the athlete now owns. `workout.CopyTo` already exists and does it in one transaction; it is reachable only through share-accept, exactly as sequences' was. (2) `router.push` after the copy stays inside the same `[id]` segment, so Next REUSES the component and `copying` survives: press Back to the original and its button is stuck disabled at "Copying…" for the life of the instance. Fix is the sequence one — `POST /v1/workouts/{id}/copy` delegating to `CopyTo`, and state keyed on the id so the flag derives rather than resets. (#294) — done #298. `POST /v1/workouts/{id}/copy`, delegating to the existing `CopyTo`, and the page's flag keyed on the id so it derives instead of surviving the navigation. **One difference from sequences worth knowing**: workouts' `visibleTo` HAS a public arm, so another athlete's published template is copyable by design where the sequence equivalent 404s — the test is table-driven over VOLA's, a stranger's and your own for that reason. The copy is always private, so copying a public template does not republish it under your name. **Review found two things worth the round trip.** The "VOLA template" fixture inserted with `source` DEFAULTING to `'user'`, so it was ownerless but not seeded — and a mutation copying `source` verbatim survived the whole suite. That one is not cosmetic: `cmd/seed` refreshes `WHERE source = 'seed'`, so a copy inheriting it would be overwritten by the next deploy, and this endpoint is the first path where athletes routinely copy seed rows. And **deriving the copy flag was not enough** — `copyingId === id` is false while you are away from the original and true again the moment you press Back, so the stuck button returned by the same route it left. Both fixed; the second also fixes the merged sequences page, which had the identical latent bug from #294.
- [x] **F11** — **Three resources wrap `CopyTo` in the same transaction dance.** `sequence.Copy` and `workout.Copy` (#294, #298) are structurally identical — begin, `CopyTo`, refuse if not visible, commit, read back — differing only in return type, error wrapper, and argument order, and share-accept does the same dance a third time for its own reasons. The duplicated part is exactly where the subtle bugs live: the deferred rollback, committing BEFORE the read-back (so a 201 never describes a row whose commit failed), and not holding a connection across the two. The next resource will write a fourth by hand. **The obvious home is closed**: `share.Copier` already declares the `CopyTo` signature, but the modules deliberately do not import `share` — the dependency runs the other way and `cmd/api` pairs them, documented in both packages. A neutral `platform` helper taking a function value keeps that intact. — done #301. `platform/database.CopySelf` holds begin → copy → commit; both wrappers go 17 lines to 6, each keeping its own `ErrNotFound`, error wrapper and `Get` argument order. It takes a **`CopyFunc`, not `share.Copier`** — same contract, without reversing the dependency the modules deliberately keep. **Two corrections to this line as filed**: share-accept is NOT the same dance (it opens its transaction so the share's status flip commits with the copy, and on a missing source DELETES the dead share and commits), so the real duplication was two, not three. And the payoff is bigger than the eleven lines: a `CopyFunc` is injectable, so the ROLLBACK finally has a test — both module suites had recorded it as untestable, because their real `CopyTo` has no natural input that fails mid-copy.
- [x] **F12** — **A hung backend test costs ten minutes to die.** Neither `test:api` nor `ci.yml` passes `-timeout`, so Go's default 10m applies per package. That is not hypothetical: F11 measured it — delete `CopySelf`'s deferred rollback and the leaked transaction keeps `pgxpool.Close` waiting forever, so the package cannot exit whatever its assertions already reported. The assertions name the cause immediately now; only the exit is slow. A bound belongs on the runner, not on one package's test, since any future leak anywhere pays the same ten minutes. Slowest package measured 2.2s locally and ~45s under the parallel-session contention CLAUDE.md documents. — done #304. `-timeout 3m` on both `test:api` and the CI step, with the reasoning in a comment beside the CI one so it is not a bare number. **Verified against the real hang rather than assumed**: reintroducing F11's leaked transaction now dies at 3m01s instead of ~10m. 32 packages pass under it, slowest 2.5s locally.
- [x] **F13** — **Two mobile test timeouts contradict each other, and the shorter one always wins.** Five component suites call `configure({ asyncUtilTimeout: 10_000 })`, but nothing sets jest's `testTimeout`, so its **5000ms default** applies and kills the test first. Measured: a `waitFor` configured at 10s dies at 5003ms with jest's message, and a plain 9s test dies at 5001ms. So those files ask for ten seconds of polling and get five — exactly the headroom they were given to survive slow renders under load. It also means **CLAUDE.md's account of the flake is wrong**: it says `sharedScreen.test.tsx`'s "10s `asyncUtilTimeout` expires", which cannot happen. Separately, the suite IS already bounded per test (unlike the Go suite before F12), so what is actually missing is a JOB-level bound — no job in `ci.yml` sets `timeout-minutes`, for any language. — done #308. `testTimeout: 15_000` in `jest.config.js`, above the 10s those files ask for, so the budget is reachable: measured, the same `waitFor` now runs 10014ms and fails on its own assertion instead of 5003ms on jest's. The ceiling still exists — a 20s test dies at 15001ms. **And the job-level bound is added**, which was the real gap: every `ci.yml` job now sets `timeout-minutes: 15` against GitHub's six-hour default, ~8x the slowest job measured at 1.8 min. Three granularities now, and only the outermost catches a hang in setup or install. CLAUDE.md's account of the flake is corrected — it named a timeout that could not fire.
- [x] **F14** — **Checked and deliberately NOT done: the web/admin vitest suites need no timeout config.** F13 left "nobody has measured vitest's default here" as an open item. Measured: vitest bounds a test at **5008ms**, the same 5s default jest has — so both suites are already bounded. And F13's actual defect cannot exist here, because **neither suite has any async waiting at all**: no `waitFor`, no timers, no promise delays, by explicit design in both config docstrings ("node environment, pure logic only"). There is nothing configured above the ceiling to be silently cut off. Adding a `testTimeout` for symmetry with jest would LOOSEN a bound that nothing needs loosened — jest's 15s exists only because five files ask for 10s. The module-scope green-hang is the same in both runners (20s of blocking reports "1 passed"; vitest at least attributes it as `import 20.02s`) and is already covered by the `timeout-minutes: 15` F13 put on all five CI jobs. (#308 #310)
## N — New work

- [x] **N1** — Grip / wrist variation (regular, neutral, reversed, angled). Belongs on the **logged set**, not the catalog: you might press neutral today and regular next week, and a catalog row per grip turns 504 exercises into ~2,000 while still not expressing "I switched on the last set". Needs a design pass. See **T3**. — done #248: `session_sets.grip`, nullable (NULL is unrecorded, never `regular`), picker on mobile gated to pushes/pulls/isolation, read-only on web. (#248)
- [x] **N2** — Find an exercise by the words you'd use. `pg_trgm` is **already installed** — fuzzy, word-order-independent matching, ~0 ms, no API. Two of three reported "missing" exercises already existed under other names. — done #255. **ALL THREE existed.** And trigrams alone fix only one of them: measured, "bench"→"press" and "overhead"→"shoulder" are vocabulary, not spelling, so a small synonym map does the other two. Ranking runs on the EXPANDED query, or a row containing every typed word outranks the movement meant.
- [x] **N3** — Fill real catalog gaps. **Zero EZ-bar entries in 504 exercises.** Sweep for others. — **closed as STALE, not done (#255).** The premise is false: there are **20** EZ-bar entries and the catalog is **762**, not 504. All three exercises reported missing exist today (Incline Dumbbell Press, EZ-Bar Curl, Seated Dumbbell Shoulder Press) — the complaint was findability, which is **N2**. Reopen with a measured gap if one is found; do not act on the numbers in this line.
- [x] **N4** — Any set can be given a duration, and a set that carries one is timed. The rule used to refuse the EXERCISE (`weight_reps` gets no timer); it now refuses only the GUESS — an explicit duration outranks the load type, nothing is invented for a set nobody timed. Circuits genuinely fall out: `canRun` consults only `workSeconds`, proved by an integration test wiring the real `workSecondsFor` rather than the stub the interval-run suite injects. A duration is a timer TARGET, not a measure — `measuresForSet` is untouched, tonnage and records are unchanged, and the completion path no longer overwrites a target with the clock's elapsed time (it still does on a plank, where seconds IS the measure). (#259)
- [x] **N5** — Weight trend on mobile: week / month / year, one tap from the check-in card at `/checkin/trend`. The seven-day mean is `lib/anthropometry.ts`'s and is NOT reimplemented — `lib/weightTrend.ts` only chooses which days to ask about and fits them to a box. A gap in weigh-ins renders as a GAP, since a line chart's default is to invent one. CLAUDE.md's platform rule gains a narrow carve-out (three conditions) rather than being weakened. (#261)
- [x] **N6** — Per-exercise load over time. Same platform-rule amendment; pairs with **N5**. **The carve-out N5 added is the precedent, and N6 has to pass its three conditions on its own** — per-exercise load is closer to analysis than bodyweight is. (#261) — done #263, **and it did NOT pass**, so it shipped on web instead of mobile. The deciding condition is the second one: the at-the-rack decision ("what do I put on the bar today") is *already* answered on the phone by the double-progression recommendation, so a chart beside it informs nothing that recommendation leaves open. What is left — "did my squat go up over three months" — is asked while planning a block, at a desk. `GET /v1/records/{exerciseID}/history` plus a chart on the records page, with the three things the carve-out forbids on mobile (metric picker, readable axes, per-point evidence) — which is the distinction being drawn, not scope creep. The carve-out is unchanged and still has exactly one instance.
- [x] **N7 (backend)** — **`POST /v1/exercises/identify` ships the shortlist half.** Vision call over the catalog's machine equipment (~200 published rows, sorted so the prompt is cacheable), returning a RANKED shortlist the athlete taps — never a selection, never a log. Built on `internal/platform/llm` with **no provider file**, which is what N36 was for. Three guards, all mutation-checked: an id that was not offered is DROPPED rather than mapped to a neighbour; a reported `equipment` that contradicts its own candidates is refused (the N40 shape — each half well-formed, the pair incoherent, invisible to an id-existence check); and the name comes from the catalog, never the response. Rate-limited as a spend bound (20/30min). **The tier is NOT yet chosen against evidence** — it starts on the capable tier deliberately, because the cost saving is the half that was never in doubt. **The mobile camera surface is NOT built** — that is **N44**, allocated and broadcast separately rather than filed inside this diff, because an id written into an open PR is invisible to `gh pr list` until it merges, which is how two sessions allocated N39 the same afternoon. — done #321 (was: Point the camera at a machine. Can't be trained on our library (8 images for 504 exercises); works as a vision call with an equipment-filtered shortlist. ~1–3 s, ~$0.005/call. **Provider decided 2026-08-19: OpenAI, not Anthropic** — judged comparable on this task and cheaper. This is a model id at the call site rather than new transport, because `internal/platform/llm` (N36, #307) carries both backends behind one `Completer` and its `Request` already has `Image`/`ImageMediaType`; `DefaultModels` deliberately stays with the consumer, so N7 picks its own tier without disturbing nutrition's or N33's. **The cost saving is not yet measured for equipment vision specifically**, and tier is the risk, not vendor: N37's dictation eval put two tiers of the SAME provider at 0.0% vs 24.2% invention (`gpt-5.6-luna` vs `gpt-5.4-nano`), so choose the tier against real machine photos before banking the difference. After logging is solid.)
- [x] **N8** — One working-set rule the copies read instead of restating. The session module's `SQLWorkingSet`, `SQLCountsAsSet` and `SQLTonnage` are exported now, and `feed` uses them in place of its **four** inline restatements. A test asserts the feed's SQL still contains the session module's rule, so a restatement that drifts fails immediately rather than waiting for a fixture that happens to notice. **The Go/SQL and the TS copies remain** — see the entry for why neither can be shared without a bigger change. (#243) — done
- [x] **N9** — Grip has no `mixed` or `hook`, so the picker is withheld from hinges, carries and olympic lifts — the movements where grip matters MOST. A deadlifter cannot record how they pull. Needs those two values plus a decision on whether `mixed` needs a side. (#248) — done #266. **`mixed` records NO side**: lifters do alternate which hand is under and it is why they care, but nothing consumes it and asking between sets collects a careless answer; #256 made adding `mixed_left`/`mixed_right` later cheap. Which grips a movement OFFERS is now `GripsFor`/`gripsFor` per pattern — and the subset is a question-quality rule, not a constraint: the server still validates only the vocabulary, because refusing an unusual-but-real pairing (a hook-gripped shrug) would invent a false negative. 93 exercises gained the control.
- [x] **N16** — The per-pattern grip table is served now: `GET /v1/exercises` carries `offered_grips` per row, derived from `movement_pattern` and never stored, so a seventh grip needs no app release — the half of #256 that was left open. **The Go copy had NO production caller**: `GripsFor`/`GripApplies` were referenced by nothing but their own tests, a specification the server published and never used while both apps re-implemented it. The table moved to `exercise.OfferedGrips` (where `movement_pattern` lives) and gained one authoritative reader, the serializer. `apps/web`'s copy is deleted outright — it fetches on render and has no cached row that could predate the field. **`apps/mobile` keeps its copy as the offline fallback only**, for exercises cached before `offered_grips` existed, and `check-grip-parity.py` now polices that one copy against the server rather than three against each other. **N16's line was wrong about one thing**: no SQLite migration was needed, because `exercise_cache` stores the whole API object as `payload_json` and the field rides along. — done #303
- [x] **N10** — Web authors grip now, not only displays it. `SetRow` takes `movementPattern` (a value, like `perSide`, not the `Exercise`), and offers a native `<select>` whose empty option is the way back to unrecorded — the half of #256's rule that is easy to drop. `emptySet` carries grip forward again: it refused because web had no control to unmake the recording with, and that premise died here. The vocabulary is now a THIRD copy (Go, mobile, web), so `check:grip-parity` compares the three `gripsFor` tables in `verify` and CI — four failure modes proven, two of them found by review (the `default:` branch, and a Go constant whose value differs from its name). It does NOT check `ValidGrip` or the CHECK, so server acceptance is a separate guarantee. (#268)
- [x] **N11** — Feed reaches back 3 days only, and the poster leads with an avatar. The window is a rolling 72h on `ended_at` inside `visibleFrom`, so the count cannot disagree with the list; the avatar is a monogram derived from the handle (no upload path, nothing new on the wire, stable colour per person). — done #252
- [x] **N21** — `contests` had a schema and no consumer: migration `000050` merged schema-first in August with "nothing reads or writes it yet — no module, no handler, no route, no client" filed as its own first gap. Now `internal/modules/contest` and five routes under `/v1/contests`. No migration — the schema was already right. Leaves the derived record ("12-4, 7 by submission") and both clients unstarted, and it is the evidence stream the accomplishments work needs. (#215)
- [x] **N23** — BJJ had no equivalent of a personal record, so a finished mat session got a summary card and no badge — `apps/mobile/app/bjj/session/[id].tsx` carried a placeholder saying so. Now `internal/modules/accomplishment` and `GET /v1/bjj/accomplishments`: seven **firsts** derived on read from `contests` and the tag stream, stored nowhere, each carrying whether its evidence is measured or reported. Backend only — #276 is mid-flight in the finish-card files, so the placeholder stays until a follow-up wires it in. Belt promotions deliberately excluded: `/v1/bjj/standing` already owns that fact. (#280)
- [x] **N29** — The BJJ finish card had no badge and a comment saying so. Now wired to `GET /v1/bjj/accomplishments` (#280): `lib/accomplishments.ts` filters the server's awards by `session_id`, the same filter-on-the-authority the PR row uses, so competition awards fall out automatically. It takes the personal record's slot **including its chime**, which means it latches the streak out — and `recordsSettled` stops being true-by-construction on BJJ. Overlaps #276 by necessity. (#284)
- [x] **N31** — **A technique drilled today now gets a live row.** *(This line originally claimed a scored tag could never name a technique at all. That was WRONG — see the 2026-08-18 history entries; the id is kept and corrected rather than reused.)* `focusRows` skipped `drilled`, so the wizard asked what you drilled on one screen and offered no way to say it landed on the next, unless the technique happened to be on the focus list — and an athlete with an **empty focus list** could attribute nothing at all, which silently made the technique funnel (`/v1/bjj/proficiency`) drilled-only and `first_drilled_scored` unreachable. Now the union includes this session's drilled techniques, named from the library (module-cached, so the ordinary step-1-then-step-2 path costs no request; falls back to the id offline). `conceded` stays out — it has no per-technique control to fill. **Two** tests pinned the old exclusion and only one was obvious. — done #291 (#284 #290)
- [ ] **N33** — **Say what happened and have it fill the chips.** A server-side `POST /v1/bjj/reflect/draft` turning dictated prose — "rolled five, hit an armbar from closed guard in the second, got passed from half guard twice" — into a **draft reflection the athlete confirms**. Never a logged session. Spec'd in [bjj-tracking-design.md §7](decisions/bjj-tracking-design.md); mirrors N26's draft-never-log rule and answers that doc's own open question 4 (transcription is on-device, via the system keyboard — the app only ever handles text, so there is no audio vendor). BJJ suits this better than food does: the target vocabulary is closed (6 categories, 5 events, 11 positions, 542 technique ids), so it is enum-mapping rather than open-ended estimation. Technique resolution puts the catalog in a cached prompt (~10K tokens at ~0.1× per call) and validates the emitted id in Go — an unknown id becomes an unresolved phrase the athlete picks, never a guess. **This is the version that removes N31's attribution cost instead of relocating it.** Blocked on a provider decision: no LLM client, key or dependency exists anywhere in this repo. Build the eval set of real dictations before choosing a model tier. (#295)
- [x] **N34** — **The dictation eval set, and the check that keeps it honest.** `evals/bjj-dictation/` — 30 cases pairing a dictated sentence with the draft a correct extraction returns, the scoring metric in the README, and a stdlib-only validator in `verify` and CI. The half of N33 that was never blocked on a provider. The metric ranks **invention rate above accuracy**: a missing tag is visible to the athlete looking at the draft, an invented one is plausible and one tap from confirmed. The validator checks every expectation against the real 542-entry catalog — an expected tag whose category or position the technique does not derive would score a *correct* model as wrong, forever, with nothing else noticing. **All 30 cases are authored, none recorded**, and the README is blunt that an authored corpus scores self-consistency rather than reality; fifty recorded dictations is the target and the count is printed on every run. No scorer yet — that needs the provider decision. — done #296
- [x] **N36** — The provider transport is `internal/platform/llm` now: `Completer`, `Request`/`Response`, `Provider`/`Valid`/`APIKeyEnv`, the two SDK calls and one pair of sentinels. `nutrition` keeps the prompt, the schema, the parse, the validation, its error vocabulary (`translateLLMError`), `DefaultProvider` and `DefaultModels` — per-feature judgements, and N33 wants a different default on the same provider. All four constraints the line named are carried and mutation-checked: the factory returns the INTERFACE (a nil concrete pointer in a non-nil interface skips the 503 and panics), validity is checked BEFORE the missing-key return, truncation maps to REFUSED on both backends, and no `effort`/`thinking` is sent. The output cap moved from a struct field onto `Request`, which made it assertable for the first time — nutrition now pins that every field it owns actually reaches the transport. — done #307
- [x] **N37** — **The dictation eval runner, and the first real scores.** `evals/bjj-dictation/run.py` + `prompt.py` — stdlib-only, one POST per case, the app's own post-processing (validate the id against the catalog, derive category and position, move an unknown id to `unresolved`) applied before scoring, because grading the raw response grades the model on fields the app never lets it choose. **`gpt-5.6-luna`: invention rate 0.0%, tag F1 0.905. `gpt-5.4-nano`: 24.2%, 0.708.** The recorded prediction was half right — nano is materially worse on invention, as written down beforehand, but did NOT match on F1, because a wrongly-resolved id breaks the F1 tuple too, so over-commitment is punished twice and the trade-off this eval was built to expose does not currently show. **The bigger finding: luna went SILENT on the direct injection** — no tags at all, the whole sentence in `note`. It did not obey, but it dropped the real armbar; that is the refusal failure mode, from the better-behaved model, caught on the first run by the case added for exactly it. **And most of run 1 was measuring the corpus:** luna went 0.652 → 0.905 across three runs with the models and prompt unchanged, because two of run 1's "inventions" were correct answers (the catalog HAS `twister-from-back-control`; "double leg" is an exact alias match) and run 2's fixes introduced two more errors. `check_resolution` is what stops it recurring — a resolved tag must name the phrase that resolved it, the phrase must appear in the dictation, and it must pick that entry out of the catalog. — done #302
- [x] **N38** — **The capture path for recorded dictations.** `evals/bjj-dictation/record.py` — `add` stashes a dictation you actually said into a gitignored `pending/`, `resolve` answers "does this phrase name one technique or seven", `promote` validates and merges, `stats` counts toward fifty. **It never calls a model**, which is the point rather than a limitation: filling `expect` from model output turns the eval into a rubber stamp. It also cannot promote an invalid case — the held path is tested. Fixed a real bug in `resolve` on the way: stopwords were required to appear in the catalog name, so "the knee cut" matched NOTHING while "knee cut" matched seven — the worst possible answer, since it reads as "not in the catalog". Stripped from both sides, so exact matches like "Armbar from Closed Guard" still hold; all four corpus guards re-mutation-tested after. **The fifty themselves are not something a session can produce** — a case written by reasoning about how an athlete talks is `authored` whoever types it, and that field is the corpus's only defence against testing itself. — done #305
- [x] **N39** — **An optional per-exercise note, admin-authored, shown in the UI when present.** (#317) W7 ruled five per-side rows a human had to settle, which fixed the COUNT and left the REASON invisible: `single-leg-kettlebell-romanian-deadlift` counts one implement while its dumbbell twin counts two, deliberately, and that read as an oversight to anyone who found it. The note carries the reason to where an athlete reads the value. Column on `exercises` (000061), served on the public read and the admin write, authored at `/content/exercises`, shown on the mobile exercise screen and the web library panel — and **nothing at all when absent**, which is the normal case (5 of 762 rows). Absent and empty mean the same thing, so `omitempty` keeps it off the wire and a falsy check is a client's whole handling. **Ownership went the OTHER way from the line that stood here**: the user chose ordinary catalog content, so the note flips `source='admin'` like any other console edit, is in both of the seeder's column lists, and IS exported into `exercises.json`. #315 had recorded the opposite — a column omitted from the seeder entirely, so annotating could not disturb deploy management — and that was reasoned from `upsertSQL` alone. The other reader of `source` is `cmd/exportcontent`, whose `AdminAuthored` is `WHERE source = 'admin'`: a note that never flipped `source` is invisible to the exporter, never reaches the seed file, is never code-reviewed and dies on a fresh database — making the explanation less durable than the ruling it explains. The flip it was avoiding is temporary and `-adopt` (`AdoptAsSeeded`) is its existing remedy. PATCH-by-omission cannot clear a note (pointer field, `exercise_media`'s guarantee), an empty note DOES clear it, and 500 chars is the cap because it renders inline on a phone with no read-more.
- [x] **N41** — **Scan a barcode, get the exact food.** The phone half: `app/food/scan.tsx`, reached from the quick-add sheet. A scan **proposes** an entry and never logs one — N26's rule inherited whole, with one deliberate difference. The describe screen shows a portion-confidence badge and the assumption behind each number; this screen shows **neither**, because those exist to mark a model's guess and a barcode's macros are printed on the packet, so painting doubt onto them would be manufacturing it. **`expo-camera` was NOT already present, and `expo-image-picker` is not a substitute** — this line asserted the first and a coordinating session then asserted the second; both were checked and both are false. The picker has **no barcode API at all**: `launchCameraAsync` captures a photo, and capture is not decode. `expo-camera` is the only thing in the tree that decodes a barcode, so this adds a native dependency, an `app.json` plugin entry, and a **dev-client rebuild** — pull this branch, run `start` against an old binary, and you get a module-load crash that reads as a bug in the screen. Its plugin adds `NSMicrophoneUsageDescription` and `RECORD_AUDIO` by default, so both are explicitly `false`, for the reason #325 records: `lib/sounds.ts` sets `allowsRecording: false` and its test calls a microphone request indefensible. **Three endings, each of which says which one it is.** Resolved → a draft. Not in the catalog → "we don't have this one", naming the digits it read, with the describe path behind it. Could not ask → says *that* instead, because "we don't have this one" is a false statement about the catalog when the truth is a basement with no signal; they are separate `Phase` states and a test goes red if they are collapsed. **Normalisation is where this silently fails**, so it is the most-tested part: the same box is 12 digits in a US shop and 13 in an EU one, and a code passed through unchanged misses on one side of the Atlantic and reports it as an ordinary miss. UPC-A widening, UPC-E expansion, EAN-8 padding and ITF-14 carton reduction all land on one 13-digit GTIN, and a **failed check digit never reaches the network** — a creased packet misreads routinely, and looking one up returns a miss that gets reported as a missing product. **Review caught that guard being vacuous in two places**: `expandUpcE` and the ITF-14 reduction both COMPUTE a check digit, so verifying their output was true by construction, and no test could see it because every vector written for those paths was valid. It now verifies the digit **as scanned**, and dispatches on the symbology `onBarcodeScanned` reports rather than guessing from length — eight digits is EAN-8 or UPC-E and the guess is what made the hole reachable. **Offline**: a resolved barcode is cached on-device in its own `barcode_cache` table (schema v20) rather than in `foods`, because `foods` pushes through the outbox and would upload a saved food nobody saved, would pollute the two-tap quick-add list, and would mix ODbL rows into ours — migration `000059` says that obligation "must never reach our own data", and `forgetOpenFoodFactsRows` is the one-statement proof it has not. A first scan needs signal and the screen says so. **An AI-drafted food stays labelled `ai` forever**, never `off` or `catalog`: N40 measured the estimator doubling a quantity at `medium` confidence with no hedge, so a drafted figure wearing a catalog row's provenance is a guess with a fact's credibility. **The lookup endpoint is N46 and is deliberately not built here** — the user scoped N41 to mobile-only — so this ships against a contract that must land before an athlete can use it. Source, shared with N42: Open Food Facts, on demand, cached, never bulk-ingested. — done #320
- [x] **N42** — **A searchable food catalog, and knowing what is actually in it.** The gap N41 lands on: a scan or a search needs somewhere to resolve to, and today nutrition has no food list an athlete can browse — N24 shipped manual entry and saved foods, which is a personal list, not a catalog. Two halves. **Availability** is the half that gets skipped and then hurts: an athlete searching "skyr" and finding nothing cannot tell whether the food is missing, the search is bad, or the region is unsupported — the same absence-reads-as-answer failure the repo has hit three times elsewhere. So an empty result must say WHICH it is, and the catalog needs a coverage answer before it ships. **Search** is the other half, and it is the one the technique library already got wrong once (#—, "the library was not missing the techniques, the search was") — read that before writing a query. Should carry the same admin-authored provenance split the exercise and technique catalogs use (`source='seed'` vs `'admin'`), so a deploy can correct a food without reverting a human's edit. Depends on the lookup-source decision in N41; do them together or N41 first. **Shipped as `internal/modules/food` + `food_catalog`, a NEW table rather than more rows in `nutrition_foods`** — that one is per-athlete (`user_id NOT NULL`, offline outbox, private by default) and a shared catalog with a nullable owner would have meant re-auditing every nutrition query. **173 foods seeded from USDA SR Legacy**, chosen because the FDC *API* is unusable for this (DEMO_KEY is 10 req/hour, measured) while the bulk download needs no key and is **frozen at 2018-04**, so the build is reproducible forever; `scripts/import_usda_foods.py` pins the FDC id of every row so any number can be checked against its source. Search is per-word and ANDed over name and per-row aliases kept APART, plus a small general-vocabulary synonym list — and ranking needed a **second signal**: trigram similarity alone put *"Lunchmeat, chicken breast"* above the actual breast (USDA's own search does this too), so lead-position sorts first. Measured 46/50 realistic queries hitting; the misses are skyr, weetabix and haggis, correctly reported as `no_match` rather than as an empty list. **The availability half is an `outcome` on every search** — `ok` / `no_match` / `query_unusable` / `catalog_empty` / `market_not_covered` — with `catalog_empty` separated from `no_match` by actually counting the table, so a deploy that never seeded can never be shown to an athlete as "we do not have that food". `market` is a column so the region answer needs no migration. Barcode lookup landed here too rather than in N41 (user's call): `GET /v1/nutrition/catalog/barcode/{barcode}`, Open Food Facts on demand, cached in its OWN table because ODbL must not commingle with authored data — and 400/404/**503** are kept strictly apart, since a network failure surfacing as "not in the database" is this repo's recurring bug at its most expensive. — done #319
- [x] **N43** (#329) — **When an athlete says "it broke", we currently have no way to see it.** The SERVER half is already good and this task is not about rebuilding it: `internal/platform/httplog` emits structured JSON via `slog`, tags every line with a service name, carries a request ID and **W3C trace-context** correlation, and is wired as middleware around the whole stack in `cmd/api/main.go`. Leave it alone. **The client half does not exist at all** — grep finds no error reporting of any kind in `apps/mobile` or `apps/web`, so a crash on a real phone is invisible to us forever, and the offline-first sync path is exactly where the hard bugs live (a rejected outbox row, a token refresh that failed in a gym dead-spot, a migration that failed on one device). Three pieces, in value order. **(1) Client error and crash reporting on mobile**, including unhandled JS errors, native crashes, and explicitly the sync/outbox failures that today only ever surface as a row that quietly never syncs. **(2) Correlation across the boundary**: the client should send the trace header the server already understands, so a user-reported problem joins to the server log instead of being two unrelated haystacks — this is the piece that turns "troubleshoot easily" from a wish into a query, and it is cheap precisely because the server side already speaks W3C trace-context. **(3) A decision on where logs actually go and how far back they reach.** Railway captures stdout, which is a stream, not a searchable history; nobody has chosen a retention window or an aggregator, and finding that out during an incident is finding it out too late. Constraints: this is health and training data, so **no PII, no note or reflection prose, and no photographs in a log line or a crash payload** — the disclosure rules N26 established for images apply harder here because a crash report is sent without the athlete initiating it; and it must degrade to silence rather than to a crash of its own when the reporter is unreachable, which is the normal state on a gym floor. Decide the vendor with the user — a hosted reporter is the cheap answer, self-hosting is not obviously worth it at this size. **Volume and cost are a REQUIREMENT, not a tuning pass, and the user stated it directly: it must not make thousands of writes, it must self-clean, and it must be efficient.** That rules out the default shape of every reporter SDK, which ships an event per occurrence. Four things it has to do rather than be configured into later: **(a) sample and cap** — errors are bursty and correlated, so one bad deploy or one device in a reconnect loop must produce a bounded number of events, not one per retry; a rate limit per device per window plus deduplication on the error's fingerprint is the shape, not a global volume knob. **(b) Batch, never per-event** — a phone on gym wifi should send one compressed batch on a timer or at a size threshold, and coalesce while offline rather than queueing every occurrence to send later, which is how an offline-first app turns a two-hour dead spot into a flood on reconnect. **(c) Levels that actually gate** — `error` and above leaves the device by default; `info`/`debug` are local-only and opt-in for a support session, because the temptation is to log the sync path verbosely and that path runs constantly. **(d) Self-cleaning is a retention DECISION, not a cleanup script** — pick the window (30 days is usually plenty for "what happened on Tuesday"), let the aggregator expire it, and make the local ring buffer on device fixed-size so it can never grow unbounded on a phone. Note the SERVER side already has the right shape here and is worth copying rather than re-deriving: one structured line per request, not one per operation. **The premise was wrong and the task was smaller and sharper than written**: `POST /v1/client-errors`, `lib/report.ts`, `RequestID`/`TraceID` capture, a 90-day `retention` with `Prune` and migration 000022, and the admin Health screen all already existed. Piece (2) was done server-side and piece (3) largely so. What was missing was that the pipeline had exactly ONE call site and was the wrong SHAPE for the crash path — one `fetch` per occurrence, so a render loop throwing 60×/s was 60 POSTs/s. **Decided with the user: first party, no vendor** — we already had persistence, correlation, retention, an admin surface and a contract entry, every SDK's default is one event per occurrence, and a crash report is sent without the athlete initiating it, so a vendor's default breadcrumbs would carry route params (session and food ids) off to a third party. `lib/telemetry.ts` is the pure buffer: coalesce, cap per fingerprint, batch, fixed-size ring, level gate, allowlist redaction — all five mutation-tested. Every suppression is COUNTED and reported (`occurrences`, `suppressed`, `lost_events`), because a reporter that quietly drops is the failure it exists to prevent. Native crash capture and symbolication are what a vendor would still buy; that is a separable later decision, not one this closes. **Web is NOT done — see N50.**
- [x] **N44** — **`app/session/[id]/identify.tsx` ships the camera surface**, reached from the add-exercise screen beside search rather than instead of it. **No new native dependency**: `expo-camera` is NOT installed and was never needed — `expo-image-picker` already ships, is already a plugin, and `launchCameraAsync` already opens the system camera in `food/describe.tsx`, so there is no dev-client rebuild and no collision with N41. **The top candidate is not pre-selected and must never become so** — that is the whole reason a shortlist comes back instead of an answer, and an athlete who confirms a pre-selected wrong row has logged the wrong exercise without ever making a choice. `confidence` is displayed, never thresholded, never re-sorted on. **Found on the way: the app declared `NSMicrophoneUsageDescription` it never uses** — `expo-image-picker`'s plugin adds it unless you pass `microphonePermission: false`, while `lib/sounds.ts` goes to the trouble of `allowsRecording: false` with a test calling the prompt "indefensible". The runtime guard was honoured and the binary contradicted it. Also gave the camera prompt real VOLA copy instead of the generic Expo default. **The quota is still the server-side rate limit only** (20/30min from N7), not a persisted daily quota. — done #325 (was: The camera surface for N7, on the phone. N7's server half (#321) is `POST /v1/exercises/identify`: a photo goes in, a ranked shortlist of catalog exercises comes back, and it never selects and never logs. This is the half an athlete touches — point the camera at a machine, get the shortlist, pick one. **Mobile-only by the platform rule**: identifying the machine in front of you is live logging, done standing up in a gym. `expo-camera` already ships in the dev client, and N26's describe-or-photograph flow is the interaction to match rather than invent a second one for the same job. Three constraints it inherits rather than chooses. **The shortlist is the product**: the athlete picks from a ranked set, so a wrong first answer costs a tap, and there is no path where the app decides for them — same draft-then-confirm rule as N26 and N41. **A photo leaves the device**, so it carries N26's explicit disclosure before the camera opens, and the same forwarded-and-discarded handling. **It spends money per call**, so it needs a quota like the estimator's — and N26's two-quota split is the precedent, since the caps there were sized against a cost ratio that turned out to be wrong by ~35x when measured. **Allocated by the coordinating session and broadcast rather than filed inside a diff**, because an id filed in a PR body is invisible to `gh pr list` until it merges — the N19 collision, and the N39 one this afternoon, were both that. Claim it in a PR TITLE before starting.)
- [x] **N45** (#333) — **`typecheck:mobile` cannot be run by two sessions at once, and the failure reads as a broken checkout.** `scripts/generate_route_types.mjs` hardcodes **port 8099**, so a second concurrent `pnpm run verify` (or `typecheck:mobile` alone) dies with `EADDRINUSE :::8099`. Metro never starts, the typed routes are never generated, and it surfaces as `Could not generate route types` — which looks like a bad checkout and is nothing of the sort. It passes on a rerun once the port is free, which is exactly what makes it expensive: it costs a confusing ten minutes and teaches the wrong lesson. **This repo's normal state is several sessions on one machine** — CLAUDE.md already documents the jest equivalent (`--maxWorkers=3`, measured over 74 runs) — so this is not an edge case, it is the default. Fix is small: bind port 0 and read back the assigned port, or take one from an env var with the current value as the default. Found by `pre-merge-checker` during N28 (#316). Note the generator itself is load-bearing and must keep failing loudly when it genuinely cannot produce types — N35 exists because a silent no-op let a button point at a route that never existed — so the fix is to stop the collision, never to soften the error. **Fixed by binding port 0 and reading the assignment back**, with a narrow retry for the probe-to-bind window and `EXPO_TYPEGEN_PORT` to pin one anyway. Measured both directions rather than one: two concurrent runs of the OLD script still reproduce it (one exits 1), two of the new one both exit 0 on adjacent kernel-assigned ports with no collision in either log. The reported symptom was not quite right and the real one is worth knowing — it is not a raw `EADDRINUSE`, it is Expo NOTICING the busy port and asking `Use port 8100 instead?`, then dying because `CI=1` makes it non-interactive. N35 is intact: a pinned-but-occupied port fails loudly with zero retries, a spawn failure fails fast, and the collision matcher is tested against the REAL captured failure text rather than an invented string.
- [x] **N46** — **The barcode lookup endpoint N41 resolves against.** `GET /v1/nutrition/catalog/barcode/{barcode}` — check the catalog, then fetch Open Food Facts on demand and cache it, then 404. **N41 (#320) shipped the entire phone half against this and it does not exist**, so every scan currently ends in "couldn't check this one". That is the honest failure rather than a broken one, and it is not something an athlete can use. The client contract is fixed by `apps/mobile/lib/barcodeApi.ts`: **200** returns `{food, source}` with `source` of `catalog` or `off`, `food` carrying per-serving macros and a `serving_label`; **404 must carry the standard `{"error":{"code":"not_found"}}` envelope**, and that is load-bearing rather than stylistic — the phone tells "we do not have this one" apart from "I could not ask" by that code, so a bare unrouted 404 or a 500 must NOT read as a missing product. There is a test on the phone side asserting exactly that, and it is the one that fails if this endpoint answers sloppily. **Open Food Facts is fetched on demand and cached, never bulk-imported**: migration `000059`'s own comment says its ODbL share-alike obligation "must never reach our own data", so the cached rows want their own table — one `TRUNCATE` from walking away with our catalog untouched — rather than a `source='off'` row inside `food_catalog`. Sits between N42 (which owns `food_catalog`) and N41. **Delivered inside N42 (#319)** rather than separately — the route, `food_barcode_cache` and the on-demand fetch all landed there, and the separate claim (#326) was closed. Kept as a ticked line rather than deleted, because it records that the gap was real: N41 shipped against a contract that did not yet exist, and for a while every scan correctly reported that it could not check. **The 404-envelope requirement held**, and the shipped handler goes further than the contract asked: `404 not_found` covers both an OFF 404 and an OFF `200` carrying `status: 0`, while transport failures get a distinct `503 unavailable` — so the phone's "we do not have this one" versus "I could not ask" split keys on the CODE and never on the HTTP status, which matters because a plain 200 from OFF can mean either.
- [ ] **N50** — **The same reporter for `apps/web`.** N43 (#329) did mobile, where the bugs are (the offline sync path) and where the task line pointed. Web still has no error reporting of any kind — no boundary that reports, no `window.onerror`, no `unhandledrejection` — so a broken dashboard is as invisible as a broken phone was. Deliberately not bolted onto N43's PR, because the honest options are a shared workspace package or a second ~380-line copy of `lib/telemetry.ts`, and this repo's duplicate-small-utilities convention explicitly covers "a ~15-line utility" and not this. Picking between them is a decision, not an afterthought at the end of somebody else's diff. The server half needs nothing: `/client-errors` already takes a batch.
- [x] **N47** — **Five follow-ups the frontend reviewer found on N44's camera screen (#325).** None was blocking, and the first turned out to be live rather than latent. **(1) `choose()` resolved the picked candidate by feeding an exercise ID into the NAME search** — `fetchExercises({ q: exerciseID })` then `.find(e => e.id === exerciseID)` — which held only because ids are slugs of names. **The filing blamed N39 for making this reachable and that was wrong: exercise renaming landed 2026-08-04 in #113** (`TestRenamingKeepsTheID`), fifteen days earlier, and N39's diff touches only the note column. So the bug was live for a fortnight, which raises its priority rather than lowering it — the first name to diverge from its slug ("Seated Cable Row" → "Cable Row Machine") makes `SearchClause`'s per-token AND-match over `name` miss, and the athlete is told **"That exercise is no longer in the catalog"** about a row the server returned two seconds earlier. Fixed with `fetchExercise(id)` against the `GET /v1/exercises/{exerciseID}` route that already existed — three lines, and *cheaper* than pulling a ranked list with media. It goes through `apiRequest`, so a **404 says the exercise is gone and anything else says something true instead**; the old path could not tell those apart and would have claimed the catalog had dropped an exercise whenever the network was down. **(2)** An identify-driven swap passed `undefined` as `fromLoadType`, so `sameShape` was always false and logged reps and weight were wiped even between two `weight_reps` machines — the screen's own comment says the full row is fetched so a swap knows whether numbers carry, and then discarded it. The replaced exercise's `load_type` is now fetched too, and a failure there falls back to the old conservative behaviour rather than refusing the swap. **(3)** Error text had no colour and no `accessibilityLiveRegion` — it rendered in the same weight as lead copy while both sibling screens use `vola.danger`, and a VoiceOver user standing at the machine was never told identify had failed, which matters most in exactly the one-handed-in-a-gym case the screen exists for. **(4) The record claimed UI that does not exist**, and the resolution is to correct the claim rather than add the number. `confidence` is per candidate and is not calibrated to correctness, so four differing numbers one beside each choice, at the moment of choosing, would read as a ranking whatever it is called — a "best match" badge in all but name, which the screen's central rule forbids. The client contract sanctions both ("display it or ignore it"), so ignoring it is a choice this screen is entitled to make and now says it makes. **(5)** A stale `retryable` could render "You can try again." under "Session not found on this device"; and an empty `candidates` array would have rendered a heading above nothing — answer-shaped, and an answer to nothing — if the 422 contract were ever violated. **The screen had no test at all**, which is why (1) survived a review that returned no blocking findings; `app/__tests__/identifyScreen.test.tsx` now covers the commit path, and all five fixes were mutation-tested. **Review of the fix then found the same defect class in the fix itself**: `accessibilityLiveRegion` is **Android-only** — which `sign-up.tsx` and `forgot-password.tsx` already record — so the comment claiming a VoiceOver announcement described behaviour that does not exist on the platform this app ships on. Paired with `AccessibilityInfo.announceForAccessibility` now, and **the identical claim was corrected in N41's `app/food/scan.tsx`**, where it was introduced hours earlier. Also from that review: the boolean `retryable` became a three-state hint, because forcing a COMMIT failure into one of two IDENTIFICATION hints reproduced the very contradiction (5) was filed to remove — a network error read "Try again when you have signal" above "Take another photo, or search instead" — and a permission denial rendered "Take another photo", advice that cannot work when there is no camera to take it with; the two independent fetches now start together rather than in series; and the commit guard is a ref, since two touches in one batch both read `null` from the render closure and `router.back()` fires twice. — done #330
- [ ] **N62** — **`fetchExercises` still hand-rolls its failure into a bare `Error`.** `lib/apiError.ts` says every module that talks to the API should throw `ApiError` — "a plain Error forces callers to pattern-match on the message, which is exactly what the API conventions forbid" — and `lib/exercises.ts` predates that. N47 added `fetchExercise` (singular) through `apiRequest` beside it, so the file now does both, and the inconsistency is deliberate rather than overlooked: migrating the plural changes the error IDENTITY for its existing callers, most importantly `app/session/[id]/add.tsx`, whose catch-then-fall-back-to-cache path is doing real offline work. Worth doing, not worth doing in passing. **The payoff is the same one N47 just bought**: a caller that cannot tell a 404 from a dead network will eventually tell an athlete the catalog dropped an exercise because their signal was bad.
- [x] **N48** — **Machine identification now has a persisted daily quota, not just an in-memory rate limit.** (#332) `exercise_identifications` (000063) meters every call; `CheckIdentifyQuota` caps it at **20 per athlete per rolling 24 hours**, and the gate runs **before** the model call — checking after would be a receipt, not a quota. Failed and refused calls count, because they spend tokens too. **The rate limiter is KEPT**: a quota bounds the day, a rate limit bounds the burst, and without the limiter a client bug spends the day's allowance in one second. Shaped as the twin of `nutrition_estimates` (000060) — adherence as a query, never a stored counter. **The gate moved into the HANDLER rather than staying in `main.go`**, which is what closes the testability gap N7's review named: `NewIdentifyHandler` had been referenced by exactly one non-test file, so the endpoint had **no handler-level test at all** and nothing could assert the route was behind its gate. It has nine now. **The 20 is a judgment, not a measurement** — nothing has measured what an identify call costs on the shipped model — but it is strictly tighter than the ~48/day the limiter allowed, and a test fails if anyone raises it above that, since a quota at or above the burst rate never binds. Left open: `humaniseWait`/`retryAfterSeconds` are now duplicated in `exercise` and `nutrition`; the third consumer should promote both into `apihttp`, which already owns the 429's shape.
- [ ] **N49** — **The estimate quotas were sized against a cost ratio that turned out wrong by ~35x, and nobody has re-derived them.** N26 split 20 text / 5 photo per athlete per rolling day, counted separately so "the dearer path cannot exhaust the cheap one" — on an assumed **~50x** cost ratio between a photo call and a text call. Measured against the live API it is **~1.4x** (0.26c text, 0.37c photo on Haiku 4.5), because the JSON schema and system prompt are ~1,500 input tokens on *every* call and dominate the image's ~1,100. So the split is now a precaution rather than the load-bearing control it was designed to be, and the asymmetry between 20 and 5 is buying almost nothing. **Two things to do, in order, and the first is the real task.** (1) **Re-measure on the model actually shipping.** The 0.26/0.37 figures are from **Haiku 4.5**; both features now default to `gpt-5.6-luna` (`DefaultModels`, `DefaultIdentifyModels`), so every cost number in this repo describes a model we no longer call by default. A week of production traffic replaces all of them with real ones. (2) **Then re-tune the caps against that**, and decide whether a separate photo cap still earns its complexity at a ~1.4x ratio — a single combined daily budget may be both simpler and more honest. Do not re-tune before re-measuring; that is how the current numbers happened. Related: **N48**, which is the larger exposure of the two.
- [x] **N51** — **Food search on mobile queried the athlete's own saved foods, not the catalog N42 shipped.** `app/food/add.tsx` searched `localFoods` — the local mirror of `/nutrition/foods`, which is the **personal** list — so on a fresh account every query returned nothing and the sheet read as broken. Reported from a real phone, which is the only place it could be found: nobody with a populated saved list ever sees it. Now searches **both**, as two sections rather than one list, because a saved food is the athlete's own and carries their serving size while a catalog row is reference data whose id lives in a different id space — and `source_food_id` is a foreign key into the personal table, so a catalog id written there would dangle. The catalog copy of a food the athlete has already saved is suppressed, deduplicated **on name** rather than id, since the two id spaces can never collide and every saved food would otherwise appear twice. **The empty state says WHICH kind of empty it is**, which is the half that would rot first: the server returns five outcomes and only `no_match` licenses "we do not have that food". `query_unusable` means nothing was asked, `market_not_covered` cannot be fixed by rephrasing, and **`catalog_empty` is OUR failure** — a deploy that never seeded — which reported as a missing food would send every athlete off to type their whole diet in by hand while nothing surfaced the real cause. A failed request is a sixth case outside the enum entirely and throws, so it can never render as an empty catalog. An **unrecognised** outcome narrows to `unknown`, never to `no_match`: the same open-at-the-server, closed-at-the-client trap N41 hit on `source`, where an unknown provider claimed to be our own catalog. Also fixed on the way: a slow answer to "chick" could render under "chicken" — answers are now paired with the query they answer, which is cheaper than cancelling the request. — done #335
- [ ] **N52** — **Capture the macros that matter, not just the four we have.** `nutrition_foods` and `food_catalog` carry `kcal`, `protein_g`, `carb_g`, `fat_g`, `fibre_g`. The user asked for the rest of what matters on a label — at minimum **saturated fat, sugar, sodium**, and worth deciding on salt-vs-sodium (labels differ by region and the app is US-first) and whether added sugar is separable. **Widening a CHECK or adding columns later is a migration**, and this schema's own comment makes the argument for declaring early — so decide the full set once with the user rather than in two passes. Note USDA SR Legacy carries far more than five nutrients per food, so the seed can populate these without a new data source; Open Food Facts also returns them per product. **The reporting half is the harder half**: N40 measured the AI estimator inventing an item and doubling a quantity while flagging only the invention, so a macro the model guessed must stay distinguishable from one read off a label. Relates to **N53**, which is where these get displayed.
- [ ] **N53** — **The Today screen shows no calories against goal, and the Food screen opens on blank pluses.** Two design asks from the user, with reference screenshots they supplied. **On Today**: calories against the day's target, and the macro split — a ring or bar per macro showing consumed against goal (`0 / 141g` protein, `0 / 238g` carbs, `0 / 63g` fat is the shape they showed), plus a streak count. **On Food**: landing on the meal sections with each one carrying its own budget and macro line — "536 calories now available · 28g protein · 48g carbs · 13g fat" — rather than four bare `+ Add` rows. The per-meal budget is the part that makes the screen useful rather than decorative: it answers *what can I eat now*, which is the question an athlete actually opens the app with. **Two honesty rules carried from N28**, and they are not optional here: an average or a total is labelled with **how many days or entries it came from**, and an unlogged day renders as a **gap, not a zero** — a zero is a claim that somebody ate nothing. Depends on **N52** for the macro set and on targets existing.
- [ ] **N54** — **Calories are not totalled on the phone.** Reported from a real device: entries log, the day total does not add up. Diagnose before designing — the totalling may exist server-side in `/nutrition/days` and simply not be read, it may be a target-not-set path rendering zero instead of "set a target", or the sum may be wrong. **Whatever it is, the fix must distinguish "nothing logged" from "we could not load it" from "no target set"** — three states that currently all render as a zero, which is the exact failure N28's reviewer caught rendering forty-two "Nothing logged" rows under an error banner. Pairs with **N53**; do this one first, because a redesigned screen showing a wrong number is worse than a plain one.
- [ ] **N55** — **A reachable server that answers badly is being reported as "you are offline".** From a real device: taking a photo showed *"Can't reach VOLA. You're still signed in — this'll load when the connection is back."* while the phone was online and staging was up (verified: `/v1/nutrition/estimate`, `/v1/nutrition/catalog/search` and `/v1/exercises/identify` all answer on the deployed API). `OfflineError` in `apps/mobile/lib/apiError.ts` is deliberately for "nothing was ever answered" and its docstring says so — so either something is throwing it when an answer *did* arrive, or the request genuinely never left, and the two need telling apart. **DIAGNOSED 2026-08-19, and it is not the 503 first suspected — the user confirms `OPENAI_API_KEY` is already set on Railway.** The cause is `netFetch` in `apps/mobile/lib/authedFetch.ts`: it wraps `fetch` in a `try` and converts **every** rejection except `AbortError` into `OfflineError`. So a TLS failure, a DNS failure, a request timeout, a body-too-large rejection and a genuinely absent network all render as the same sentence. **A photo upload is the likeliest request in the app to fail at that layer for a reason that is not connectivity** — it is the largest body and the slowest round trip. The fix is to stop collapsing them: distinguish a timeout, a size rejection and a transport failure from "no route to the API", and let anything that is not genuinely a connectivity problem say what it was. The old suspicion, kept because it is still true of the endpoint: **the estimate route serves 503 when no provider key is configured** (by design — every other route is unaffected), and a 503 is an answer, not an outage. That must read as *"photo logging isn't switched on yet"*, never as a connection problem. **And the copy is too long for the moment it appears in** — the user's words were "the error itself is ugly". It is three sentences on a screen somebody is holding one-handed over a plate.
- [ ] **N56** — **One trend presentation, used by weight, per-exercise load, and every trend after them.** The user supplied two references (competitor screenshots, described here because the files are on a Desktop and the description has to outlive them). **The compact card**: title left, a delta right reading `↓ 13.3 lbs past year`; a line chart with the FIRST point labelled in a filled callout (`220.5`) and the LATEST labelled in a second callout (`207.2`) sitting on its point; a **dashed projection** continuing past the last real reading toward a **dashed horizontal goal line** labelled with the target (`175`); three x-axis labels only (start, middle, today); a `TODAY 207.2 lbs` block bottom-left and a primary action bottom-right (`Record Weight`). **The full page**: same chart larger, a range row of chips — `1W 1M 3M 6M 1Y All Plan` — a delta with its since-date, a full-width primary action, a plain-language projection sentence (*"Based on your current plan, you'll reach your goal on Monday, Jan 18, 2027"*), and an **Entries list** below with date, an optional photo thumbnail, and the value. **READ THE PLATFORM-RULE CONFLICT ON THE N57 LINE BEFORE BUILDING THE FULL PAGE** — the compact card is uncontroversial, the full page is not.
- [x] **N57** (CLAUDE.md amended) — **RULED by the user 2026-08-19: widen the carve-out.** They asked for the richer trend card and said of the one we ship, "pretty much useless". They were right — `checkin/trend.tsx` is 105 lines with **no axis labels, no value labels and no point labels at all**, because the rule as written forbade all three. The rule was meant to keep *analysis* off the phone and instead produced a chart nobody can read a number off, which answers no question in three seconds and sends the athlete to a desk anyway — the exact outcome the carve-out existed to prevent. **Struck from the first bullet: "no axes to read values off, no tooltips and no zoom".** A mobile trend chart may now carry value-readable axes, first/latest point labels, a dashed projection to a dashed goal line, a delta against a stated period, and an entries list. **Unchanged and still disqualifying: a second metric, or a date-range picker** — a control choosing a start AND an end, which is comparison and stays on web. Preset windows ending today were always allowed. The other two bullets are untouched. **So N56's full page is now permitted on mobile.**
- [ ] **N58** — **Food search results should be scannable, not a wall of names.** Reference: each result is a card with a **food emoji or icon** at the left, the food name (wrapping to two lines when long), the **brand** beneath it in muted text (`Maxima`, `Tesco`, `Joytofu` — omitted for generic foods), a serving line reading `182 cals per 100 Grams` or `456 cals per Serving`, and a **circular `+`** on the right that adds without opening the detail. Above the list: a **scope row** — `All / My Foods / Meals / Recipes` — and a `Results` header with a **verified-only filter** on the right. **The emoji is the ask that needs design work**: it must be derived, not authored per row across 177+ USDA foods and every OFF product. Options worth costing: a category→emoji map (USDA food groups already exist in SR Legacy), keyword matching on the name, or an emoji column on `food_catalog` populated for the seeded set with a category fallback for everything else. **Whatever it is must degrade to a neutral glyph rather than a wrong one** — a steak emoji on a tofu product is worse than no emoji. Depends on **N51** (the search being wired at all).
- [ ] **N59** — **The food detail screen should show a real nutrition panel, and the add-food entry should group its three routes.** Reference for the panel: `Amount` row with an editable quantity and a **unit picker** (`100` / `Grams`), then a two-column block with the calorie number large on the left and the label breakdown on the right — **Total Fat, Sat Fat (indented), Cholesterol, Sodium, Total Carbs, Fiber (indented), Sugars (indented), Protein** — with `n/a` shown for values we genuinely do not have rather than `0`, which is the absence-reads-as-answer failure in its most literal form. Below: `View Full Nutrition Label`, then a **LOG DETAILS** group (`Log Time`, `Mark as Planned`, `Meal`) and an **OPTIONS** group. A sticky bottom bar carries quantity, unit and confirm. **Requires N52's wider macro set** — sat fat, sugar and sodium are on this panel and are not stored today. Separately: **group the three ways to add a food** — photograph, describe, scan a barcode — into one presented choice rather than three unrelated entry points, which is how they exist now.
- [ ] **N60** — **BJJ voice logging has no screen. N33 (#322) is backend-only — zero files under `apps/mobile`.** It ships `POST /v1/bjj/reflect/draft`, `bjj/reflect_drafter.go`, the prompt, the quota, migration `000063` and the OpenAPI entry, and **nothing an athlete can touch.** The user went looking for "BJJ logging with AI and voice" on a real phone and correctly found nothing. **This is the third instance of one pattern today**: N42's catalog shipped with no mobile search (N51), N7's identify shipped with no camera surface (N44, since built), and now N33. A server route is not a feature. What the screen needs: a text field the athlete dictates into **using the system keyboard's own microphone** — transcription is on-device by design, the app only ever handles text and no audio ever leaves the phone or reaches the server; a **draft the athlete confirms, never a logged session**, matching N26's describe-a-meal flow rather than inventing a second interaction for the same job; unresolved phrases surfaced as **a pick-one prompt, never a silent guess** (the server validates every technique id in Go against the 542-row catalog and hands back unresolved text deliberately); and the counts — rounds, events — made **easy to correct**, because N40 measured this class of model flagging what it invents while stating a miscount flatly, and *"rolled five"* becoming six is the error that survives review. **The eval corpus is 33 authored / 0 recorded**, so it is blind to keyboard transcription mangling — a green eval is not evidence this works on real speech, and the screen is where that gets found out.
- [ ] **N61** — **The belt roadmaps and every other BJJ surface are invisible unless the BJJ module is enabled, and nothing says so.** `(tabs)/workouts.tsx` gates `CurriculaStrip` on `modules.some(m => m.enabled && m.capabilities.catalog === 'techniques')`, and the Library tab's syllabus row is gated the same way. The gate itself is right — it is deliberately a capability predicate rather than `key === 'bjj'`, which is the better pattern. **The problem is that a fresh account sees nothing and is told nothing**: the user reported "roadmaps curricula are not there" from a real phone, and the screens exist and work. An athlete cannot distinguish *not enabled* from *not built* from *broken* — the same absence-reads-as-answer failure this repo keeps paying for, here costing a feature its entire discoverability. Needs either a visible path to enabling the module from where the content would be, or an empty state that names the reason. Worth auditing the other module-gated surfaces in the same pass rather than fixing this one instance.
- [ ] **N12** — Real uploaded avatars. The monogram is the fallback layer either way, so this is additive: needs `profiles.avatar_url`, the platform `objectstore` wired up, an upload surface, resizing, and a moderation answer — `display_name` is already unguarded prose friends can see and a photo is worse. (#252)
- [ ] **N13** — The 3-day feed window is not configurable and not surfaced beyond one line of copy. If athletes want a week, it is a constant (`feed.FeedWindow`) plus the copy that names it in three places. (#252)
- [x] **N14** — `sessions` now has `sessions_user_ended_idx (user_id, ended_at DESC) WHERE ended_at IS NOT NULL`. The 3-day window was a Filter over every friend's lifetime of training: measured at 200k sessions / 500 users, **4000 rows fetched, 3919 removed by filter, to return 81** — cost scaling with how long somebody has trained rather than with their week. Now an Index Cond: 81 fetched, 515 buffers → 49. Guarded by a test that reads the query PLAN, since every correctness test passes either way. (#258)
- [x] **W6** — The rest of the catalog has never been re-read against the *separated* facts. `implements` was backfilled from the old derived rule, so every row outside the lunge family inherits whatever that rule said — including any other movement held with two implements while working one limb (carries, some presses). The lunge family was found by internal contradiction; nothing has swept for the rest. (#257) — done #260. One row (`single-leg-dumbbell-romanian-deadlift`), and the reason nobody found it: the name guard's `single-` matched `single-leg`, pinning a two-dumbbell movement at x1 — the same limb/implement conflation 000057 removed from the rule, surviving in the test meant to protect it. The guard also still derived the factor the OLD way, so it had stopped checking anything real.
- [x] **W7** — **A human ruled all five rows, and they are pinned now.** `bottoms-up-kettlebell-press` was the unsafe one and it moved on BOTH fields as this line predicted it would: x2 → **x1** and `is_unilateral` false → **true**, so it stops inventing double the weight actually moved AND starts telling an athlete "8 reps means 8 each side". `double-dumbbell-kickstand-deadlift` → `is_unilateral: true` (implements stays 2 — two implements, one leg, tonnage untouched). The other three are **confirmed unchanged**, which is a state change rather than a no-op: they move from "judgement call nobody has confirmed" to ruled, and only that justifies pinning them. `single-leg-kettlebell-romanian-deadlift` **deliberately stays x1 while its dumbbell twin is x2** — the ruling took the counter-reading below, so the cross-implement disagreement F3 was opened for is resolved by decision, not inference; do not "fix" it back. All five now live in `TestTheRuledRowsStayRuled`, pinned on both fields, because the name heuristic can see none of them. — done #311 (was: Four per-side rows are judgment calls nobody has confirmed, and names cannot settle them: `bottoms-up-kettlebell-press` (x2 — **the one on the unsafe side**, since x2 INVENTS tonnage if wrong. Its `is_unilateral=false` corroborates the two-bell reading, so the row is internally coherent as a double bottoms-up press; if it is wrong, both fields are wrong together. Take this one first), `dumbbell-pistol-squat` and `kettlebell-pistol-squat` (x1; commonly one counterbalance weight, sometimes two), `single-leg-kettlebell-romanian-deadlift` (x1). **That last one is closer to provable than the others, and THIS branch created it**: correcting the dumbbell single-leg RDL to x2 left the kettlebell one at x1, which is a dumbbell/kettlebell disagreement of exactly the shape F3 was opened for. The cross-implement argument says 2 — the parent `dumbbell-`/`kettlebell-romanian-deadlift` pair agree at x2. The counter-reading is that the dumbbell side splits into two rows (bare + `one-arm-`) while the kettlebell side has only one, which may be intended to BE the one-bell version. Decide with both in view. Note it is currently unguarded in both directions — deliberately, since pinning a disputed row is the `renegade` mistake — so any edit to it passes silently until this is resolved. Each doubles or halves an athlete's tonnage. **A fifth row belongs here, on the other axis**: `double-dumbbell-kickstand-deadlift` says `is_unilateral: false` while both its single-implement siblings (`dumbbell-kickstand-deadlift`, `kettlebell-kickstand-deadlift`) say `true` about the same stance — the flag-off-to-buy-the-doubling trade W5 reversed for the lunge family, surviving outside it, so the double variant loses its "8 reps means 8 each side" hint. W6 asked for a re-read against the *separated* facts and this sweep read the `implements` axis only; `is_unilateral` still carries old-rule residue, which matters because the bottoms-up argument above leans on that field as corroboration. Wants a human, not another inference.) (#260)
- [x] **W8** — The weight chart's SELECTED range pill had no visible label: `accent.ink` on an `accent.accent` fill, i.e. the accent colour written on itself. The provider's own doc says `on` is what may be written on a fill. Found by looking at it on a Simulator — a colour is invisible to typecheck, lint and every test in the suite. (#264)
- [x] **N15** — The search synonym map is hand-maintained lifting vocabulary (`exercise/search.go`). It is small and closed by design — it grows when a word is ambiguous in the gym, never per exercise — but nothing checks it against the catalog, so a synonym pointing at a word no row contains is silently dead. — done #255, filed and closed in the same change: review pointed out it costs one test and no database. It immediately found three dead entries, two of them written that hour. (#255)
- [x] **N19** — A week that met its plan, and the streak rungs, are marked instead of passing silently. Two halves: Today's `WeekReview` shows a congratulation when `metThePlan(review)` — `weekVerdict` already ended "the whole plan, done" but said it in the same grey sentence as every other week; and the finish card shows a rung crossed at 4 / 13 / 26 / 52 consecutive weeks, with a chime that now outranks the personal record (frequency argument inverts at that rung — the top one happens at most once a year). **`WeekReview`'s "no score, no grade, no streak" note stands and is not overturned**: `milestoneReached` returns a rung only in the week it is newly reached (`===`, never `>=`), so there is no running number on any screen to protect or to break. The Progress tab keeps the honest count for anyone who goes to look. — done #276

- [x] **N17** — The belt roadmaps are unreadable to a novice: no map of the sport, no legible progression across the four belts, and goals stated as the implication of a paragraph rather than its first line. **Done.** A `The map: how a round goes` phase leads white belt — the route, the loop back, the positional hierarchy, position before submission — four concepts, no techniques, no criteria, and white belt only, because a round's shape is learned once. Every roadmap opens with a `Where this belt sits` concept naming all four jobs, and the belts are renamed so the four titles read as a progression; both render through the concept card the clients already have, so this cost no UI work. The content itself was NOT the problem and the phase titles were already right — renaming them onto the new domain table would have traded "Half guard is a position, not a failure" for "Guard: attacking". New: `docs/content/bjj-curriculum-structure.md`, the twelve canonical domains and the two-artifacts-one-vocabulary rule that **N20** is built from. (#272)
- [x] **N18** — Draw the map instead of describing it. **N17** shipped it as four concept cards, which is strictly worse than the flowchart it describes, and the graph to draw it from already exists: `technique/positions.json`, 11 ordered positions each with a `description` and a `priorities` paragraph. Wants the route as a diagram with each node linking into the library filtered to that position — full version on web, compact read-only on mobile (reference material, not analysis, so the N5 carve-out is not what governs it). Note the name collision to avoid: `bjj/positions.tsx` is already the athlete's own *position heatmap*, which is a different thing entirely. (#272) — done #274, on **both** platforms. **Authored, not derived, and that was the finding**: the library really does carry a position graph (`to_position`), and deriving from it gives the right spine and then falls apart — only 170 of 542 techniques record one, so there is no side-control-to-mount edge, no mount-to-back and no escapes at all. A drawn map with holes reads as "you cannot get there". So the structure is authored teaching content and everything it points at is real, checked by a test that cross-references both embedded catalogs with no database. Nodes are **sided** where the glossary is not — "sweep" and "get swept" would otherwise be the same arrow — so a node carries `position_id` (glossary) and `position` (library filter), and two nodes may share the first. Served on the existing `/v1/techniques/positions` response rather than a new endpoint, so a client cannot hold a map and a glossary from different versions. Web draws the graph; mobile draws the same content as a **ladder**, since `tier` carries the hierarchy — the half a beginner most needs — with no edges required. **Review earned its keep twice**: the `knee-on-belly` node was silently borrowing side control's technique list (the library has no KOB `position` value — it lives on `position_detail`), which every check passed because they only asked "does this resolve to something"; and both clients' library deep link carried a glossary id to chips keyed on **family**, so the feature's headline link filtered the grid to nothing with no chip looking active. The second fix deleted the whole params mechanism rather than repairing it — a node links to the position itself now, counted with the same helper the destination uses. (#274)
- [x] **N20** — The reference syllabus layer. The proposal N17 came from names ~70 techniques for white belt against the roadmap's 25, and that gap must NOT be closed in the roadmap — 70 criteria-bearing items is a progress bar nobody fills. It belongs in a `syllabus` track: complete coverage, match order, **no criteria on anything**, sharing section vocabulary with the roadmap so an athlete moving between them never meets a name they have not seen. Pure content: every technique the proposal names already resolves in the 542-entry library (checked name by name, including `de-ashi-barai`, `turtle-sitout-escape`, `foot-pummel-guard-retention`, `reverse-de-la-riva-kiss-dragon`, `crab-ride-back-take`), and a wholesale reseed cannot touch athlete progress — with one boundary review flushed out: that holds for reorder/rename/re-describe/add, but **removing** a technique item moves the displayed fraction, since its countable and mastered contributions leave with it (the record survives, and re-adding restores it). The open question was whether `track` alone carries it or whether a curriculum needs to say it is un-completable in its own right. **Neither: `countable_items` already did.** Both clients' `isRoadmap` switch predates this and needed no change, so a criteria-free curriculum was already handled — the work was asserting the property rather than adding one. **Done #277**: white 73 techniques / 11 sections, blue 71 / 9, purple 42 / 10, brown 35 / 7. Curated rather than derived even though `typical_belt` covers 100% of the catalog and derivation was available — a derived complete list IS the Library's belt filter reorganised, and would have been 158 white-belt items: a catalog rather than a syllabus. (A second reason given at first, that this breached `MaxItems`, was **wrong** — review checked, and that bound gates only the client write path.) Two tests hold the distinction from both ends. Web-only, and the mobile strip now filters by TRACK not belt, or a 73-item list that finishes nothing would have appeared as a roadmap card wearing a belt photograph. **Review found two things the guards missed**: `novice-fundamentals` was already exempt from both (third track), so the vocabulary is asserted closed now; and the new "Keep this handy" button invited bookmarking a syllabus, after which Today announced **"Every technique on this one is done."** over "0 of 0 mastered" — `Working` now excludes anything with nothing completable, as a display rule rather than an un-enrolment. (#272 #277)
- [x] **N22** — **The map's edges drove through the boxes they passed.** Found the only way it could be: by rendering the real geometry against the real content and looking at it. Six crossings, every one an edge spanning more than one row — "on their turtle" to "their back" ran through mount AND north–south, and three escape edges ran through the pin above them. #274 shipped with the layout unit-tested and this invisible to all of it. Multi-row edges now route through a reserved GUTTER lane down each side, at a fixed x rather than an offset scaled by rows — the first attempt scaled it and put a three-row edge hundreds of pixels off-canvas, correct and half invisible. **The default view was also wrong**: `route` alone meant every edge touching the losing band was hidden, so the four worst positions rendered with no arrows at all, floating and reading as broken — the same "a missing edge is invisible" failure the concede kind exists to prevent, reintroduced by a default. Now route + recover, concede opt-in. Three permanent tests over the REAL content: no edge crosses a box, no edge leaves the canvas, no node floats in the default view. (#274 #281)
- [x] **N24** — Nutrition, the last unbuilt MVP pillar: calorie and macro targets derived from the live body phase, plus manual food entry and saved foods/recipes. Backend only; nothing user-visible. **Half the requested checklist was already shipped** — `body` covers weight, girths, private photos and goal pacing, and `anthropometry.ts` already computes the rolling average — so this is the part that genuinely did not exist. The load-bearing rule is that a logged row **owns its numbers**: `source_food_id` is provenance and no query returning nutrition may follow it, or correcting a saved food silently rewrites every entry ever logged from it with nothing left to compare against. Targets are keyed on (user, date) so a day logged in March is judged against March's target. The derivation runs server-side — its dominant input is a 28-day training average the phone does not hold — and **refuses** when the resting rate would come from `energy`'s generic baseline, which runs 20–30% high and would overfeed by ~400 kcal/day invisibly. `check:rate-parity` joins `verify` and CI because the rate bands are now a second copy of `anthropometry.ts`'s. (#278)
- [x] **N25** (#283) — Nutrition on mobile: a fifth `Food` tab (gated on the new `has_food_log` capability, `href: null` — never by omitting `<Tabs.Screen>`), the day screen, a two-tap quick-add from recents, the Today card showing **remaining** kcal and protein, and the offline outbox at schema v18. Two outbox shapes, not a third: entries are push-only (copy `sequences.ts`), foods need a pull too (copy `workout_cache`). **Also in scope and easy to miss: a screen to start a body phase.** `createPhase`/`endPhase` exist in `lib/body.ts` and in the API with zero UI callers anywhere, so today there is no way to begin the cut a target derives from. Explicitly not built: per-meal calorie allocation, exercise-calories-back, streaks, a "Done Logging" toggle, or any chart — each candidate fails the N5 carve-out on its own terms.
- [x] **N26** (#287) — Describe it or photograph it: a server-side `POST /v1/nutrition/estimate` that turns "two eggs, sourdough and butter" — or a photo — into an editable draft the athlete confirms. Never a logged entry. Structured outputs against a JSON schema carrying a per-item `portion_confidence`, because portion size from a photo is genuinely unreliable and the model must be able to say so. Text-first: it covers most logging at a fraction of the cost. Needs a per-user quota (this is the one endpoint where a loop costs real money) and an explicit disclosure that a photo leaves the device. **This is why the food-database work is unscheduled** — if describing a meal in a sentence works, a food database is largely redundant. **Shipped with two quotas, not one** — 20 text and 5 photo per athlete per rolling day, counted separately so the dearer path cannot exhaust the cheap one. **The ~50x cost ratio this was designed around was wrong** — measured against the live API it is ~1.4x (0.26c text, 0.37c photo on Haiku 4.5), because the JSON schema and system prompt are ~1,500 input tokens on every call and dominate the image's ~1,100. The split is now a precaution rather than the load-bearing control, and the caps are worth revisiting. Failed and refused calls count, since they cost tokens too. The gate runs BEFORE the model call, asserted by a test that checks the model was called zero times. Photos are forwarded and discarded, with the disclosure on the screen before the camera opens. `ANTHROPIC_API_KEY` is optional: without it this one route serves 503 and every other nutrition route is unaffected. (#287)
- [x] **N40** — **The first real plate photograph through the estimator.** N26's bake-off was four inputs and one SYNTHETIC image, and its own entry recorded that "no real photograph of a real plate has been through either" — so the refusal boundary was measured and accuracy on real food was not. One now has. `gpt-5.6-luna`, 11.5s, six items, 955 kcal, against ground truth written down before the model saw the image. **Four clean hits, one quantity doubled, one item invented.** The invention (40 g of "cured meat", 180 kcal — almost certainly the tomato in the hash) came back `portion_confidence: low` AND named in the top-level note as unclear, so the draft flagged its own worst row. The doubled egg did not: "2 large fried eggs" where there was one, marked `medium`, reading confident. Net ~29% over on calories, and **the unflagged error is the dangerous one** — a phantom row is visibly wrong to an athlete correcting a draft, a `2` where there was a `1` is one glance from being accepted.
- [x] **N27** (#306) — The weekly target adjustment rule. Compare the observed 7-day trend-weight change against the phase's target rate and propose a kcal delta. The guards are the whole feature, not decoration: ≥10 of 14 days logged (adherence as a **query**, never a stored counter), ≥4 weigh-ins in each half, ≥14 days on the current target, a 0.25%/week deadband, and a step capped at min(10%, 250 kcal). Always a proposal with the arithmetic shown; never a silent auto-apply, and no cron — this repo has none.
- [x] **N28** (#316) — Nutrition on web (`/dashboard/nutrition`): recipe authoring, target setting with its derivation shown, correcting a past day, and the analytical surface — intake bars against the target line, the 7-day mean, weight trend on a second axis, training days marked underneath. That join is the thing no competitor can do. Two honesty rules: an average is labelled with how many days it came from, and unlogged days render as **gaps, not zeros**. **Shipped following `apps/web`'s existing tokens, not `apps/admin`'s design system** — reconciling the two is still separate, not-yet-started work, and a third style would have made it harder. **First and only client of N27's adjustment endpoint**, which shipped with none: the proposal renders its own arithmetic line by line and is accepted by a button, never applied. Both honesty rules live in `lib/nutritionSeries.ts` as arithmetic rather than in a component, and are mutation-tested from both ends — zero-filling the series turns 11 of 25 pure assertions and 5 of 6 render assertions red, and making the chart draw every day or span its gaps turns 3 of 6 render assertions red. **No food-catalog picker, deliberately**, so N41/N42 land in an empty seam rather than replacing a thin one.

- [x] **N30** — The reference syllabuses were unreachable on the phone. #277 left them web-only on a platform-rule argument — the roadmap is the worked path, the long-form reference is read at a desk — and that **does not survive the obvious comparison**: this app already carries the entire 542-technique library, searchable, on the Library tab, so excluding a curated 73-item belt list was inconsistent rather than principled. They now sit in the Library's reference block beside the position glossary and the round map, widest-first: the map (the shape of a round), then the belts, then one position. They stay OFF the Plan tab's Roadmaps strip, which is the half of #277's reasoning that holds — Plan is what you are working and these finish nothing. Selection is a tested helper (`lib/syllabuses.ts`): syllabus track only, `editable` excluded so an athlete's own public curriculum cannot pose as VOLA content, belt rank not alphabetical. **Known and unmeasured**: the curriculum screen is a plain `ScrollView` and the white belt syllabus is 85 items — double the largest roadmap and this screen's old ceiling — recorded at the call site with the threshold for revisiting, and review agreed the call is right (85 rows of text is a different regime from the virtualised 542-row catalog). **Review found one blocking regression**: the new `listCurricula` call omitted `ac.signal`, which disarmed the block's 10s captive-portal deadline for the glossary sharing its `Promise.all` — my stated premise that the function took no signal was simply false. It also found a test that could not fail (the unknown-belt branch was unreachable behind a filter that dropped unknown belts) and the `editable` misconception now filed as **F7**. (#277 #282)
- [x] **N32** — The Food target's only button called `router.push('/profile')`, a route this app has never had — there is `/profile/edit` and no index beside it — so the one action on a screen explaining why it cannot compute a target landed on `+not-found`. **Nothing could have caught it**: Expo Router's typed routes are generated by Metro into a gitignored `.expo/`, so CI's `tsc` sees a loose `Href` and passes; it surfaced only in a worktree that happened to hold a stale generated file. Routing it at `/profile/edit` would have been a half-fix, because one of the four things the target waits for is a **weigh-in**, which is not on that form — `profileGap` picks the screen from what is actually missing. `lib/__tests__/routes.test.ts` now checks every navigation literal in `app/` and `components/` against the real route tree, which is the part that runs in CI. — done #292
- [x] **N35** — Expo Router's typed routes are generated in CI now, so `tsc` actually checks route literals. `typedRoutes` was already on and `tsconfig.json` already included `.expo/types`, but that file is written by the **Metro dev server** into a gitignored directory — so a clean checkout had none, `Href` fell back to a loose string, and every route literal passed unread. That is what let N32 ship. `scripts/generate_route_types.mjs` starts the dev server, waits for the file and stops it (3.5s local, 5.5s on the CI runner), chained into `typecheck:mobile` so CI needed no workflow change. Measured, not assumed: there is no `expo typegen` in SDK 57, `expo export` does NOT write the types (~21s, nothing), and `@expo/cli` is unresolvable from `apps/mobile` under pnpm's strict layout. Proven end to end from a clean tree — reintroduce `/profile` and `typecheck:mobile` exits 1 naming the file. — done #297

## L — Recorded, low

- [ ] **L1** — Nothing on the phone has been seen on a phone: share card, feed, drop indent, `↳`, Assisted field placement. Typechecked and tested, never looked at. (#210 #227) **Now also N4's timer field and the play button it puts on a squat** — built and tested, never seen on a device. (#259) **And N5's weight chart** — an SVG whose readability genuinely cannot be inferred from a passing test. (#261) **And N10's grip select on web** — a control added to a dense table, never rendered. ~~**And N29's BJJ accomplishment badge**~~ — **seen on a Simulator 2026-08-18** against staging: the pill renders "FIRST DRILLED TECHNIQUE LANDED LIVE" on the BJJ finish card, correct session, beside the streak line. The evidence had to be inserted by hand to get there, which is what surfaced N31. Note the label is the longest of the seven and the pill uppercases it, so it wraps to two lines — legible, but worth a look next time somebody is in that card. **The rest of this line is still unseen.** (#268 #284 #286) **F6's set editor is the first entry to come OFF this list**: the pills, the collapse, the hold-for-info panel, the Timed switch and the `+ Set` button were all seen on a booted Simulator against this branch's own Metro (#275). The `+ Set`/`+ Drop` pair and the minutes-mode `0.5` case are confirmed too, once the Simulator's latched hardware keyboard was cleared — **by `simctl shutdown` + `boot`, not by ⌘⇧K**, which needs the Simulator's own window focused; the global `ConnectHardwareKeyboard` pref read `0` the whole time and is not a discriminator. **Only VoiceOver is still unseen**, and it is the part this component's design leans on hardest.
- [ ] **L2** — A drop dragged away from its parent re-parents. Inherent cost of expressing the relationship as order, since row ids are regenerated every save. (#227)
- [ ] **L3** — `dropsOf` has no consumer. Mirrors the server for whatever reads a drop group next; dead code until then. (#227)
- [ ] **L4** — 1RM and tonnage read `weight_kg` differently. Deliberate (a per-hand 1RM is what lifters quote) but nothing tells a client. (#224)
- [ ] **L5** — Share leaves a ~1.6 MB temp file per share. **Both halves of the old reason were wrong**: it said "needs a third native dependency" (`react-native-view-shot` already exports `releaseCapture(uri)`, native on both platforms) and priced it at ~1–2 MB when the capture was actually exporting 10.5 MB. Size fixed in #232; cleanup still not done, because calling `releaseCapture` after `shareAsync` resolves needs device verification that every share target has finished reading by then. (#210 #232)
- [ ] **L6** — Instagram Stories direct hand-off. No longer blocked (`vola://` is ours now); what remains is a Facebook App ID, an account decision. (#210)
- [ ] **L7** — Adjacency is unenforced: nothing stops a client writing a stray `drop` row. It's skipped rather than misattributed. (#226)
- [ ] **L8** — `apps/web`'s `describeSet` has **zero callers** and formats raw `${weight}kg`, ignoring the athlete's unit preference. Dead and wrong at once; delete it, or give it units and a caller. (#241)

## H — Housekeeping

- [x] **H1** — `TestEverySeededTechniqueExistsInTheLibrary` skipped in CI (CI never seeds), so the suite reported success while the assertion never ran. **Fixed by reading the EMBEDDED technique catalog instead of querying `techniques`**, so it runs everywhere, needs no database, and cannot skip. Both sides now also fail rather than pass vacuously on an empty file. Note the database version was the *weaker* check: a hand-seeded local DB also holds admin-authored rows, which would satisfy an id no fresh deploy has. (#216)
- [x] **H2** — Document that `-p 1` also enforces cross-package test ordering, not just global counts: `session` alone against an unseeded database fails 23 tests. Documented in #228 (with an interim `TestMain` guard), then **fixed** rather than left documented: `session` (#231), `workout` (#234) and `profile` own their catalog rows now, and every module package passes alone against its own pristine database. `-p 1` is back to isolation only, and the rule is enforced structurally: `exercise`'s seeding tests now delete the catalog after themselves, so there is nothing left to borrow and a reintroduced id fails in the ordinary CI run. (#228 #231 #234)
- [x] **H3** — Drop stale local databases. **Done:** 11 databases → 2, 139 MB → 25 MB. Dropped `vola_merge` (v33), `vola_mig` and `vola_test_contests` (no `schema_migrations` at all), and `vola_test_{body,energy,library,load,pr207,setdetail}` (v39–53, all behind main's 000054). Kept `vola` (the local dev database) and `vola_test` (shared, current at 54). All had zero connections and none matched an in-flight worktree. **Diagnosing this reproduced the trap CLAUDE.md warns about**: `vola_test` at 54 against a checkout showing 53 looks like somebody's unmerged migration reached the shared database — but 000054 is on main (#248) and the *primary checkout was three commits stale*. Check whether the version exists on `origin/main` before reaching for the rollback recipe.
- [x] **H4** — Mobile tests flake under load: **reconciled, and it is not load.** #235 (0 failures/92 runs) and #233 (1-in-3) were both right and measured different things. Measured across 74 full-suite runs: one jest instance never fails, *even at load 89 under CPU saturation* (0/12); **three concurrent instances fail 8% (2/24) at load 69** — lower load, more failures. The variable is worker oversubscription, not CPU: jest sets no `maxWorkers`, so each instance claims 9 on a 10-core box and three claim 27. Suite wall time goes 6.5s → 33.7s and the 10s `asyncUtilTimeout` at `sharedScreen.test.tsx:135` expires with the card still rendered. Capping each instance to 3 workers: **0/18 failures and nearly twice as fast** (15.9s). Not a CI risk — CI runs one instance — so nothing was changed in jest config; the mitigation is guidance in CLAUDE.md for parallel sessions. (#232 #233 #235)
- [x] **H5** — Give `technique`'s seeding tests the same cleanup `exercise` has. **Done.** Five full-library seeding sites, not the two a `Seed(ctx` grep finds — three more load it via `UpsertAll(ctx, SeedData())`, and guarding only the two left 542 rows behind with the suite green; the row count found them, the grep did not. The cleanup removes the seeded curricula first because every FK into `techniques` is CASCADE, so a bare delete succeeds and silently guts them (measured: 136 curriculum items → 38, suite green). Side effect: H1's test now skips consistently instead of skipping on a fresh database and passing on a reused one. `positions` (11 rows) left deliberately.
- [x] **H6** — Claim a task before working it. **Done:** `gh pr list --state open` before starting, then an empty commit + draft PR titled `[claim] <ID>` before writing code. Chosen over a claim field in this file because TASKS.md *is* the contended resource — a claim written here is one more edit to the file two sessions are already fighting over, needs a push to be visible anyway, and conflicts more. Written down in CLAUDE.md, and this PR claimed itself that way to prove the mechanic works. It does not close the window between deciding and claiming, and nothing enforces it — but the check half already paid for itself: a session picked up **H1**, ran `gh pr list` first, found #216 open with the work done but a week stale, and landed that instead of writing a second copy. (#242)

---

## Done

Move nothing here — mark in place above. This section is for whole efforts, not individual lines.

- [x] Per-side dumbbell load — every dumbbell set was counting at half its real value (#224)
- [x] Spotter reps + drop sets, backend (#226) and phone (#227)
- [x] BJJ position map (#213), SQLite transaction serialisation (#214), contests schema (#215)
- [x] Session share card, feed cards, `share_training_details` (#210)
- [x] Check-in card width (#211), `.clerk/` gitignore (#212)
